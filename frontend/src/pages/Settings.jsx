import { Typography } from 'antd'

const Settings = () => (
  <div>
    <Typography.Title level={4}>系统设置</Typography.Title>
    <Typography.Text type="secondary">店铺配置与API密钥管理（开发中）</Typography.Text>
  </div>
)

export default Settings
