# 2026-04-13 工作内容 — 老林（系统架构师）

---

## 一、今日完成（25 个 commit）

### 1. Ozon 数据采集全面改造（v1→v4）

- **v1**：Performance API 异步统计流程（提交UUID→轮询→下载）
- **v2**：发现 Seller API `/v1/analytics/data` 能拉 revenue+orders，但流量指标全 deprecated
- **v3**：发现 Performance API `/api/client/statistics/json` + `/report` 端点能返回完整广告数据（曝光/点击/花费/订单/收入）
- **v4 最终版**：双 API 组合
  - Seller API 同步秒回：revenue + orders（用户立即看到）
  - Performance API 后台补充：impressions/clicks/spend（分批5个活动）
- **Ozon client_id 修复**：之前为空导致 Seller API 403，补上 `2727539` 后通了

### 2. WB 出价端到端验证通过

- 服务器直接调 `approve_suggestion`：₽50 → ₽65 成功
- 3 秒后回读确认：`bid_search=65.0, bid_rec=65.0`，unified/combined 一次改两个

### 3. 分时调价大量改进

- **出价系数限制**：前端 0-200 整数，后端同步校验
- **启用后立即执行**：enable 接口内调 `execute()` 填充 ad_groups + original_bid
- **SKU 状态表**：只显示 active 活动 + 无 ad_groups 的活动也出现（LEFT JOIN）
- **启用/关闭遮罩 Modal**：分步提示（保存配置→启用并执行→刷新状态）
- **UI 不刷新页面**：用 `localEnabled` 本地 state 立即切换按钮和状态表
- **实际出价记录**：WB min bid 自动重试后用实际值（如 ₽80）而非目标值（如 ₽48）
- **关闭恢复原价**：日志类型从 `user_manual` 改为 `time_restore`
- **ad_groups 唯一索引**：`uk_campaign_sku(campaign_id, platform_group_id)` 防止重复

### 4. 数据同步改为后台执行

- WB 和 Ozon 统一后台线程执行，前端立即返回"后台同步中"
- 解决 API 429 限速导致超过 nginx 120s 超时 → 504 的问题
- 更新数据源 Modal 遮罩 + 同步结果展示

### 5. 其他修复

- AI prompt 最低出价限制 + 安全护栏 60% 下限
- 建议列表过滤已暂停活动
- 手动执行后自动刷新调价历史

---

## 二、当前数据状态

| 平台 | 记录数 | 天数 | 曝光 | 点击 | 花费 | 订单 |
|------|--------|------|------|------|------|------|
| WB | 540 | 7天 | 189,011 | 6,193 | ₽12,285 | 138 |
| Ozon | 49 | 7天 | 0 | 0 | ₽0 | 91 |

Ozon 广告指标为 0：Performance API 后台线程有数据库连接 bug（`Packet sequence number wrong`），待修。

---

## 三、遗留

### A. Ozon Performance API 后台线程数据库连接 bug

`_start_perf_background` 用新 `SessionLocal()` 但报 packet sequence error。需要改连接方式或用独立连接池。

### B. WB/Ozon 均无小时级广告统计 API

- WB fullstats 只有天级（无 hour 字段）
- WB `/adv/v2/stats/hourly` 返回 404（不存在）
- Ozon Performance API 也是天级
- `ad_stats.stat_hour` 暂时无法填充

### C. 分时调价关闭时出价恢复不完整

上次关闭分时调价后，部分 SKU 平台出价没恢复（WB API 可能静默失败），导致再次启用时看到的"当前出价"和"原始出价"不一致。

---

## 四、今日 commit 清单（25 个）

```
35156dc 11:46 feat: AI prompt增加最低出价限制
1502d8b 11:50 fix: 建议列表过滤已暂停活动
7e01f6b 11:55 feat: 执行后自动刷新调价历史
b15eca8 12:15 refactor: Ozon采集器改用Performance API
f7100de 12:21 fix: Ozon一次性提交所有活动ID
4eeb303 12:23 feat: 更新数据源Modal遮罩
a309901 13:04 refactor: Ozon采集器改用Seller API
eb528fd 13:14 fix: Ozon只请求revenue+ordered_units
060db26 13:28 feat: Ozon采集器v3 Performance API JSON报告
a1f8544 13:37 fix: Ozon数据同步后台异步执行
9b39b88 13:50 fix: Ozon一次性提交所有活动ID
51c90ae 13:55 feat: Ozon采集器v4双API组合
38bb4da 14:05 fix: 出价系数限制0-200整数
9dee309 14:10 fix: SKU状态fallback显示
276a30b 14:15 fix: SKU状态只显示active活动
c12eb92 14:22 feat: 启用后立即执行+刷新
869b81c 14:30 fix: 移除setEnabled
59e7bf4 14:33 fix: 不触发onSaved
7868b6e 14:36 fix: 记录实际执行出价
376c2d3 14:50 fix: time_restore枚举
a1eb238 14:57 fix: localEnabled切换
b803658 15:01 fix: 关闭不调onSaved
b3e3582 15:07 feat: 遮罩Modal进度
2091f12 15:23 fix: 统一后台执行
```

---

**老林**  2026-04-13
