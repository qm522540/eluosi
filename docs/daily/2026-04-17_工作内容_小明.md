# 2026-04-17 工作内容 — 小明（前端）

> 本日志覆盖 04-16 下午至 04-17 的全部前端工作（04-16 上午已有单独日志）。

---

## 一、今日完成

### 1. 广告管理菜单 + 页面重构

- 左侧菜单"广告管理"改 SubMenu → **推广信息** `/ads` + **出价管理** `/ads/bid-management`
- 页面内 Tabs 去掉，按 URL 直渲染 AdsOverview 或 BidManagement
- **平台 + 店铺下拉合并为单个 OptGroup**（与商品列表一致），去掉两步选择
- 删"自动化规则 / 数据分析 / 预算管理"三个未开发 Tab，物理删除孤儿文件（`AdsRules.jsx` / `ComingSoon.jsx` / `AdsAnalysis.jsx` / `AdsBudget.jsx`）

commits: `5948ffa`、`a959039`、`b90b1c2`

### 2. 活动详情 - 商品出价行展开关键词

- 每行商品可展开 → 子表显示该商品绑定广告组的关键词（关键词 / 匹配类型 / 出价 / 状态）
- 链路 `sku → listings 查 listing_id → adGroups 找 group → getKeywords(ad_group_id)`
- 未绑定广告组 / 有组无关键词 → 友好 Empty 态 + "去添加"按钮跳转广告组 Tab
- 商品单元格大图 56x56 + 标题 + 等宽 SKU Tag
- 顶部新增统计条：商品数 / 平均出价 / 已绑定广告组比率
- 活动详情抽屉宽度从 900px → 85% 屏宽
- WB 广告活动关键词展示（活动级，所有 SKU 共享）
- 关键词加 CPC/CPM 列 + 商品出价显示商家编码

commits: `68e934e`、`234d0ea`、`dfaf76e`、`4dfa069`、`df4158a`、`5911068`、`873f037`

### 3. 商品管理页改进

- **编辑弹窗 → 85% Drawer 双栏布局**：左栏基本信息 + 右栏平台同步数据（原价/销售价/库存）
- 编辑弹窗基本信息行三列：卖家编码 + 平台编码 + 本地分类
- 品牌/俄文名改只读（从平台同步不可编辑），商品列改为中文名主 + 俄文名副
- **铺货 Modal → Drawer 85% 双栏 + 3 项 AI 调整开关**
- 描述字段加 AI 优化按钮，标题 AI 直接填入
- 成本价加 extra 提示"本地字段，不同步平台"
- 库存列新增（后端同步时回填 stock）
- 商品图片和标题可点击跳转平台商品页
- 商品列表 SKU 行前显示平台商品 ID
- 删除"重置"按钮（鸡肋），keyword 输入框加 allowClear
- 移除商品列表删除按钮（危险操作，不应在列表页暴露）
- 必须先选店铺才显示商品列表
- 商品编辑弹窗排版重构 + 图片显示升级

commits: `536b5b7`、`58b3c3a`、`6ce59a2`、`ac08615`、`7e852e9`、`25c4c2f`、`ab62be4`、`4518a51`、`77b77f5`、`7ef6dbe`、`d5fff36`、`19dc434`、`830cca1`、`2dc4383`、`707947e`

### 4. AI 调价前端改进

- **SKU 级忽略/恢复开关**（二次确认弹窗）
- 广告活动加"付费类型"字段（CPM/CPC/CPO），不匹配的活动排除在 AI 调价外
- 数据粒度提示 "保留40天" → "保留45天"

commits: `91599d1`、`d867e54`、`008002f`、`91eefef`、`55c0d9f`

### 5. OSS 图片归档

- OSS 依赖 + 客户端工具 + 配置骨架
- 归档接口 + 编辑弹窗加 OSS 图片归档按钮

commits: `6c470f7`、`f136bed`、`2368fec`

### 6. 全局 UI 改进

- **时间统一转莫斯科时区显示**（前端 formatMoscowTime 工具函数）
- Yandex 店铺表单收窄为单一 API Key 字段（修复"测试连接 Key 不对"）
- 编辑弹窗 Form.Item 加业务提示文字
- 同步时回填重量

commits: `d9d6f52`、`33a6e58`、`2645c2d`、`50348fe`

### 7. 商品 products 长宽高 + 平台属性（进行中）

- products 表加长宽高字段
- GET /products/{id}/platform-attributes 接口
- 前端 api 加 getProductPlatformAttributes（step 3 未完成）

commits: `8c5859d`、`75bc5ba`、`0e88c89`

---

## 二、修改文件一览（主要）

| 文件 | 说明 |
|------|------|
| `AppLayout.jsx` | 广告管理 SubMenu（推广信息 + 出价管理） |
| `App.jsx` | 新增 /ads/bid-management 路由 |
| `ads/index.jsx` | 去 Tabs 壳 + 合并平台·店铺下拉 |
| `AdsOverview.jsx` | 商品行 expandable + 关键词子表 + 美化 + WB 关键词 |
| `BidManagement.jsx` | SKU 忽略/恢复 + 付费类型过滤 |
| `Products.jsx` | 编辑 Drawer 双栏 + 铺货 Drawer + 库存列 + 大量改进 |
| `Settings.jsx` | Yandex API Key 单字段 + 莫斯科时间显示 |

---

## 三、待联调

1. **商品 platform-attributes 接口**：`0e88c89` step 3 未完成，前端 api 已加但 UI 还没接
2. **OSS 图片归档**：前端按钮就绪，后端 OSS 配置（AK/SK/Bucket）需部署到服务器环境变量
3. **映射管理线上联调**：028 迁移已部署，真实数据已有（4 分类 + 5 品类映射 + 142 属性映射），等刷浏览器验证
4. **campaign-products listing_id**：老张已加（`cfbe95f`），前端已对接，待验证展开关键词链路是否真实跑通

---

## 四、明日计划

| # | 任务 | 优先级 | 备注 |
|---|------|--------|------|
| 1 | 完成 platform-attributes 前端 UI（step 3 剩余） | 高 | 已有 API，差 UI |
| 2 | 映射管理线上联调验证 | 高 | 028 已部署 |
| 3 | 商品出价关键词展开 → 线上实测 | 高 | listing_id 已返回 |
| 4 | OSS 归档实测（等服务器 env 配好） | 中 | 依赖运维 |
| 5 | 主包 chunk 治理：路由级 React.lazy | 中 | gzip 808 kB |
| 6 | 铺货弹窗目标店铺改读接口（当前硬编码） | 低 | |

---

**小明**  2026-04-17
