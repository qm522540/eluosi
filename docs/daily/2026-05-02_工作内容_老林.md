# 2026-05-02 工作内容 — 老林

> 今日主线：**04-28 遗留收 4 条 + 店铺克隆从 0 到全栈上线**
>
> 上午: 2 个安全 bug 修 + 死 import 收尾 + Yandex source_key 封存 + 新业务规范 v1→v2 定稿 (~1170 行) + migration 061+062 草案 + 老张 review 9 条全采纳
>
> 下午（用户拍"立刻开干"绕过老板拍）: migration apply 生产 → providers (Ozon 真实+WB/Yandex stub) → task_service 19 函数 → scan_engine + publish_engine (Ozon /v3/product/import) → API 16 路由 + Celery 2 beat → 前端 3 页 (任务列表/待审核/日志) → 后端 + 前端双部署上线
>
> **19 个 commit + 2 次生产部署 + 30+ 文件改动**。明早 8 点跟老张端到端联调。

---

## 一、启动恢复 + 三天空档说明

按 CLAUDE.md 流程读了：
- `docs/daily/2026-05-01_工作内容_老张.md`（昨日老张 — 4 个 SEO bug 修 + shop=6 PT.Gril 真稀疏确认 + 数字语义讲清楚）
- `docs/daily/2026-04-28_工作内容_老林.md`（我自己最近一份 — B6 漏扫补完 + 解除 WB 静默 5 个 beat）
- `docs/api/bid_management.md`

**三天空档（4-29 / 4-30 / 5-01）我都没写日志**：4-29/30 离线，5-1 老张写了我没写。git log 显示这两天范围我个人无 commit，今天补回。

---

## 二、主线 6 段

1. **04-28 遗留清单 6 项收 4 项**：2 个安全 bug 修 + 死 import 清理 + Yandex source_key 评估
2. **新业务"店铺克隆"规范从 0 写到 v2 定稿**（用户提需求 → 5 决策对齐 → v1 → 老张 review → v2 → 老张拍板）
3. **migration 061 + 062 草案占位 + 字段类型对齐 BIGINT UNSIGNED**（老张 review 抓出 INT vs BIGINT JOIN 索引失效问题）
4. **跟老张 3 次密集协作**（trip hook 终极方案降级判断 + v2 9 条 review 回复 + a167196 frequency 膨胀同步部署）
5. **下午用户拍"立刻开干"绕过老板拍**：migration apply 生产 + 后端全栈 (providers/task_service/scan_engine/publish_engine/api/celery beat) + 前端 3 页 + 双部署上线
6. **Ozon /v3/product/import 真实上架** Phase 1.1 已知 dimensions/weight/vat 占位（待联调发现真问题再加 95014 阻止 approve）

---

## 三、19 个 commit + 2 次生产部署

### 3.1 fix(ads): `manual_sync_shop` 加 `get_owned_shop` — 多租户隔离漏洞（commit `d2e0c02`）

文件：`app/api/v1/ads.py:2164-2237`

**原 bug**：`db.query(Shop).filter(Shop.id == shop_id).first()` 无 tenant_id 过滤，攻击者枚举 `shop_id` 可触发任意租户的店铺广告活动同步。规则 1 + 规则 4 双重违反。

**修法**：
- 删 `shop_id: int` 路径参数 + `current_user=Depends(get_current_user)`（函数体未使用）
- 加 `shop=Depends(get_owned_shop)` 守卫（dependencies.py:48 已有，自动校验 tenant 归属）
- 函数体内 `shop_id` 全部替换为 `shop.id`，`shop` 直接拿到守卫返回的对象
- 删 `if not shop: raise 404` 死分支（守卫已处理）

**部署**：老张今早 deploy 合并到生产 ✓

### 3.2 fix(ad-auto-exclude): `excluded==0` 不再误标 partial（commit `b86c0ce`）

文件：`app/tasks/ad_auto_exclude_task.py:357`

**原 bug**：`rec_status = "failed" if err else ("partial" if excluded == 0 else "success")`
0 屏蔽词的语义是"今天没词需要屏蔽"（业务正常），不是"部分成功"。data_source_management 健康卡片会把跑成功的活动标黄给用户惊吓。

**修法**：`rec_status = "failed" if err else "success"`
err 走 failed 分支，excluded==0 直接 success。partial 语义保留给真正"做了一部分但有问题"场景。

**部署**：老张今早 deploy 合并到生产 ✓

### 3.3 docs(api): store_clone v1 规范（commit `274f3e4` / 946 行）

文件：`docs/api/store_clone.md`

**新业务**：用户提"智能跟店"需求 → 命名定为"店铺克隆" → 5 决策对齐：
1. Phase 1 = 自营双店 (seller token); Phase 2 留口公开 API
2. 待审核 + 一键批准（不自动推上架）
3. 跟"上新" + `follow_price_change` 开关跟价
4. 每日 MSK 03:30 + 手动触发
5. 同 SKU 永久跳过 + 跳过明细记日志
6. 图片下载 OSS（方案 B）

**v1 文档结构（14 章）**：
- 路线图（Phase 1/2/3）
- Provider 抽象层（Phase 2 不返工的关键）
- 4 张表 + 1 个 ALTER（不动 platform_listings.status ENUM 避免污染 8 文件查询）
- scan / publish / price_sync 完整流程伪码
- 接口规范 ~16 个 API + 错误码 95xxx
- AI 改写复用 SEO 现成接口 (`optimize_title` / `generate_description`) 的严格约定
- 类目映射兜底（Phase 1 缺失即 reject）
- 9 个后端文件分工 + 接口→函数映射
- 前端 3 页契约

### 3.4 chore(tasks): 4 文件死 import 清理（commit `0d50f2d` / B6 收尾）

文件：`app/tasks/{daily_stats,daily_sync_task,roi_alert,ad_auto_exclude_task}.py`

我 04-28 commit `e240ead` 把 11 处 `datetime.now(timezone.utc)` → `utc_now_naive()` 时保守保留了 `from datetime import` 行，本次精确确认 4 文件实际依赖再清理：

| 文件 | 实际用 | 删 |
|---|---|---|
| daily_stats.py | date, timedelta | datetime, timezone |
| daily_sync_task.py | 都不用 | 整行删 |
| roi_alert.py | 都不用 | 整行删 |
| ad_auto_exclude_task.py | timedelta | datetime, date, timezone |

**待下次 deploy 携带**（老张说放在他 silent-detector 实战观察后一起合）。

### 3.5 docs(api): store_clone v2 — 老张 9 条 review 全采纳（commit `892119d` / 1170 行）

老张 review 9 条按严重度排序：

**P1 决策（卡实现起点）**：
1. 草稿 product_id 来源 → A 方案：每条 pending 同步新建 products + listing 占位，SKU 命名 `clone-pending-{task_id}-{src_sku_id}`，状态机 `inactive → active(published) / deleted(rejected) / inactive(restore)`
2. providers 按平台分文件 + factory 工厂：`base.py + factory.py + ozon.py + wb.py + yandex.py`，与 `app/services/platform/` 现有结构对齐

**P2 流程缺漏**：
3. Phase 2 UNIQUE NULL 共存 → §3.2 加 TODO 块（generated column 改造方案）
4. 首次扫描窗口 → `since = last_check_at or (created_at - 7 days)`
5. AI 改写并发 + 失败兜底 → `asyncio.Semaphore(5)` + fallback 到 source 原文 + `_ai_rewrite_failed_*` 标记前端展示，错误码 95013
6. reject 不可逆 → 双保险：API `POST /pending/{id}/restore` + 前端二次确认弹窗，错误码 95012

**P3 文档补完**：
7. providers 必走 `*Client` 不写 HTTP 不绕过 quota cooldown — 写死强制约定
8. clone_published_links source_shop_id 反查不冗余 — §4.3 加约定

老张验证通过 #9 (AI 接口签名) + #10 (不动 status ENUM) 不动。

### 3.6 feat(migration): 061 + 062 草案占位（commit `0638274`）

仅本地占位 + 未 apply，远端零接触。等老板拍 v2 后跑 `deploy_remote.sh --migrate`。

文件：
- `database/migrations/versions/061_platform_listings_clone_task_id.sql`
- `database/migrations/versions/062_store_clone_tables.sql`

撞号检查通过（老张 04-18 加的 pre-commit hook 起作用 ✓）。文件头注释含合规自查（规则 1 + 规则 6）+ Phase 2 改造 TODO 提醒。

### 3.7 refactor(migration): 字段类型对齐 BIGINT UNSIGNED + ORM clone.py（commit `e364019`）

老张 review 抓出关键问题：草案用 INT id + BIGINT tenant_id（缺 UNSIGNED），跟现有核心表（platform_listings/shops/products/tenants/task_logs）的 BIGINT UNSIGNED 不对齐。

**关键风险**：JOIN platform_listings.clone_task_id ↔ clone_tasks.id 类型不一致 → MySQL implicit cast → 索引失效 → 60+ 万 listing 表 JOIN 性能塌。

修法：
- 061 ALTER 改 BIGINT UNSIGNED
- 062 全表 id/tenant_id/shop_id/task_id/listing_id/pending_id/reviewed_by 全部 BIGINT UNSIGNED
- ORM `app/models/clone.py` 同步：4 模型类，所有 id/task_id/listing_id 用 `BigInteger`，时间字段 `default=utc_now_naive`（规则 6 唯一入口），`detected_at/reviewed_at/published_at` nullable 不带 default → service 层显式传
- clone_logs 加 `idx_tenant_log_type` 索引（老张 review 加，UI 按租户+类型筛走此索引）
- detail JSON 注释补 review/publish/price_sync 三种 log_type 示例

### 3.8 feat(errors): 加 95xxx 段 13 个错误码（commit `a44cb90`）

按规范文档 §7 加：CLONE_TASK_NOT_FOUND / DUPLICATE / INVALID_CONFIG / SOURCE_INVALID / SCAN_RUNNING / SOURCE_API_FAILED / PENDING_NOT_FOUND / PENDING_INVALID_STATUS / PUBLISH_FAILED / CATEGORY_MAPPING_MISSING / TARGET_SHOP_INACTIVE / PENDING_NOT_REJECTED (95012 restore 校验) / AI_REWRITE_FAILED (95013 sem=5 失败 fallback)。

### 3.9 feat(schemas): app/schemas/clone.py 入参 + 响应辅助类型（commit `c265856`）

7 个 Pydantic schema：
- 入参：CloneTaskCreate / CloneTaskUpdate（含 model_validator: target≠source + price_mode=adjust_pct 必填 pct）/ RejectRequest / PendingPayloadUpdate / BatchActionRequest (max_length=100)
- 响应辅助（IDE 文档参考非强制）：ScanNowResult / AvailableShop / CategoryCoverageItem / PendingSourceSnapshot / PendingProposedPayload（含 _ai_rewrite_failed_* 标记位）

字段范围严格按规范 §5.1.1 §3.2：price_adjust_pct [-50, 200] / default_stock [0, 999999] / batch ids 1-100。

### 3.10 fix(migration): 062 SQL 语法 — Python 字符串拼接 MySQL 不支持（commit `5b29b0c`）

apply migration 时 ERROR 1064 暴露：proposed_payload COMMENT 多行用 Python 习惯隐式拼接 `'a' 'b'`，MySQL 当语法错。合并到单行修复，立即重 apply 通过。

**经验教训**：CLAUDE.md 启动恢复后我没本地跑 SQL 语法检查（项目无 Python 环境），靠 deploy 时撞了才发现。下次新 migration 写完先用 `mysql -e` 干跑一次验证语法。

### 3.11 feat(clone): providers 抽象 + 工厂 + Ozon 真实 + WB/Yandex stub（commit `d6364d2`）

按规范 §2 §11，5 个文件：
- `base.py` — `BaseShopProvider` ABC + `ProductSnapshot` dataclass
- `factory.py` — `get_provider(db, source_shop)` 按 platform dispatch
- `ozon.py` — 完整实现，走 `OzonClient.fetch_products / fetch_product_info / fetch_product_descriptions_batch / fetch_product_attributes_batch`，组装 ProductSnapshot
- `wb.py` / `yandex.py` — stub `NotImplementedError`，等 Ozon 跑通再实现

强制约定：providers 内**必须**调 `*Client._request`，不允许自己写 HTTP（绕过 04-24 quota cooldown 防护会重新撞 trip）。

Ozon 字段映射（参考 product/service.py 现有用法）：
- offer_id 作 B 平台 SKU；name → title_ru；price (string) → Decimal
- stocks.stocks[].present 聚合 → stock；images + primary_image 去重
- description_category_id → platform_category_id（与 028 表对齐）

### 3.12 feat(clone): task_service.py — 19 函数 CRUD + approve/reject/restore + 列表/日志/辅助（commit `b981aa6`）

覆盖规范 §5 全部 service 层：
- §5.1 任务管理 7 个：create/list/get/update/enable/disable/delete + scan-now 包装
- §5.2 待审核 6 个：list_pending / approve / reject / restore / update_pending_payload (merge 非替换) / batch_approve / batch_reject
- §5.3 日志 1 个：list_logs
- §5.4 辅助 2 个：list_available_shops（含 has_seller_token 检测）/ check_category_coverage（基于 clone_pending_products 实际 missing 统计）

**状态机三方同步**（规范 §3.3）：reject/restore 时 product + listing + pending 三表 status 一起改。

合规：所有 query `.filter(*.tenant_id == tenant_id)`；时间字段 `utc_now_naive()` 写、ISO 8601 'Z' 标 UTC 渲染；错误码 ErrorCode.CLONE_*。

### 3.13 feat(clone): scan_engine.py — _run_scan 核心（commit `08ecdf8`）

按规范 §4.1 完整实现：
1. `factory.get_provider` 拿 B 店 Provider
2. `since = task.last_check_at or (task.created_at - 7 days)` 首次扫描窗口
3. 分页 `provider.list_products(cursor, limit=100)`
4. 对每 snapshot：查重（UNIQUE 约束 + 显式查避免 INSERT 抛异常）→ 类目映射（同平台直接复用 / 跨平台走 028 双向反查 source→local→target，缺失则按 strategy 跳过）→ 价格规则（same / adjust_pct）→ 创建草稿 product+listing → OSS 图片 → 暂存 source 原文（AI 改写延后批量）
5. AI 改写批处理 `asyncio.Semaphore(5)` 并发限流 + 失败 fallback 到 source 原文 + `_ai_rewrite_failed_*` 标记位（前端展示角标）
6. 写 clone_logs.detail 含 skipped_skus 明细 max 200 防 JSON 过大
7. UPDATE clone_tasks SET last_check_at / last_found_count / last_publish_count / last_skip_count

### 3.14 feat(clone): publish_engine.py — Ozon /v3/product/import 真实上架（commit `0182ac0`）

走 OzonClient._request → /v3/product/import：
- new offer_id 用 `clone-{uuid8hex}` 占位（Ozon 接受任意字符串作商家 SKU）
- attributes 透传 B 店原属性 + 4180 (名称) + 4191 (描述) 必填兜底
- vat='0' / depth=width=height=100mm / weight=100g 占位（用户上架后必到 Ozon 后台改否则 24h 内被下架）

成功路径：listing.status inactive → active + sku 回填 / product.status inactive → active / pending.status approved → published / INSERT clone_published_links / 写 clone_logs(publish success)
失败路径：pending.status approved → failed + publish_error_msg / 写 clone_logs(publish failed) / 用户可在前端编辑 proposed_payload 重 approve

WB / Yandex 平台 Phase 1 留 stub（return CLONE_PUBLISH_FAILED）。

### 3.15 feat(clone): API 16 路由 + Celery 2 beat + 注册（commit `57cb89f`）

新增 3 文件：
- `app/api/v1/clone.py` — 16 路由 (Pydantic 入参 + Depends(get_owned_shop)/get_tenant_id 守卫 + service.* 调用 + success/error 包装)
- `app/tasks/clone_tasks.py` — 2 个 Celery beat:
  - `daily_scan_all_tasks` (MSK 03:30) 扫所有 is_active=1 调 _run_scan
  - `publish_approved_pending` (每 5 分钟) 扫 status=approved 调 _publish_pending
- task_service.scan_now 包装 _run_scan 给 POST /scan-now 入口

修改：
- `app/api/v1/router.py` 注册 `/api/v1/clone`
- `app/tasks/celery_app.py` include clone_tasks + 加 2 个 beat schedule

### 3.16 后端部署 → bash deploy_remote.sh --backend ✓

```
Python 语法预检通过
ecommerce:celery-beat / celery-worker / fastapi 全 RUNNING
健康检查 200 一次过
```

### 3.17 feat(clone): 前端 3 页 + 路由 + 菜单 + ESLint 修复（commit `c8b8599`）

按规范 §12 实现极简版：
- `frontend/src/api/clone.js` — 16 接口 client（拦截器 reject 风格，不双保险判 code 与 feedback memory 对齐）
- `frontend/src/pages/clone/CloneTaskList.jsx` — 任务列表 + 新建向导 Modal 含 5 决策表单 + 启用/停用 + 立即扫描 + 跳转待审/日志 + 删除
- `frontend/src/pages/clone/PendingReview.jsx` — 主战场，左 B 原图右 A 改写后双栏对比 + Tabs 切 pending/approved/rejected/published/failed + 批量勾选 + 批量批准/拒绝 + 单条 approve/reject (二次确认)/restore/编辑 payload + AI 改写失败角标 + 类目缺失高亮
- `frontend/src/pages/clone/CloneLogs.jsx` — 日志时间表 + 任务/类型/状态过滤 + detail JSON 详情弹窗
- `App.jsx` 加 `/clone/*` 路由 / `AppLayout.jsx` 加"店铺克隆"主菜单 (CopyOutlined) + 3 子菜单

ESLint 修一轮：empty block + unused import + unused 'e' 改 '_e'（0 errors / 4 warnings 通过）。

### 3.18 前端部署 → bash deploy_remote.sh --frontend ✓

```
vite v6.4.2 build: 3747 modules, dist 3MB / gzip 956KB
nginx 直接 serve 远端 dist/，无需 reload
```

---

## 四、04-28 §九 遗留清单状态收尾

| 项 | 状态 | 备注 |
|---|---|---|
| ✅ B6 漏扫 11 处 utc_now_naive 替换 | 04-28 已修 | commit e240ead |
| ✅ ad_auto_exclude excluded==0 → success | 今日修 | b86c0ce |
| ✅ ads.manual_sync_shop 加 get_owned_shop | 今日修 | d2e0c02 |
| ✅ 4 文件死 import 清理 | 今日修 | 0d50f2d 待 deploy |
| 🟢 ~~bid_management.py:91-95 缺 tenant_id~~ | **假阳性** | 6 处 SQL 全已带 tenant_id，Explore agent 当时定位错 |
| 🟢 Yandex catalog + sync_products source_key | **评估封存** | catalog.py 整个 yandex 平台未纳管，做了也没人用（Token 还没拿到），等 Yandex 上线再做 |
| 🟡 schema_migrations 跟踪表 | 长期架构债 | 仍未做 |
| 🟡 WB per-seller quota counter v2 | 长期架构债 | 仍未做 |
| 🟡 quota 可见性 4 子项 | 等老张 deploy 后 24h 盯 | 健康卡片 / trip notification / failed notification rate-limited / MSK 06:00 日报 |

**手头 P0/P1 已清干净。**

---

## 五、跟老张 3 次密集协作

### 5.1 04-28 trip 事故 hook 终极方案 → 决策降级 P2

老张早晨 sync 提"WBClient._request 顶层拦截 hook"累计 4 天积压，要我对方案。

**我的判断 + 数据**：让老张先验过去 4 天 task_log + Redis cooldown TTL，结果：
- 4 天 0 trip + 0 silent-quota Redis key + task_logs 全 success
- 04-28 那套 `WBSellerQuotaExhausted(BaseException) + 双 key + _request 预检` 防护**已经够用**

**拍板**：hook 终极方案 P0 → P2 降级（不叠床架屋）。

**老张 5-1 新观察**：跑 PT.Gril 30 天聚合时 31 批全返 0 行但 HTTP 全 200 + 0 429 + Redis 静默——这是 WB 的"silent rate-limit"（限流不报错只挂数据），跟 trip 是两种不同物种，cooldown 防护抓不到。

**对接**：
- 同意作为独立需求 P3 silent-detector
- 让老张起 `reference_wb_silent_rate_limit.md` memory 记账（已起，含物种特征 4 条 + 与 trip 的区分表 + 推荐 detector 实现层取舍）
- 拍 detector 写**业务层** `search_insights/service.py`（不污染 `_request` 签名）

老张今天写完 silent-detector commit `38c878e`，按我给的约定 6 条全实现：
- WB_SILENT_BATCH_THRESHOLD=3 / WB_SILENT_DAY_TRIGGER_LIMIT=5
- sleep 30→120s
- type=silent_rate_limit + silent_quota=True
- errors 数组兼容现有 task 层"errs 非空 → partial"
- 不自动重试，清晰"放弃当天 + 报告"

**建议先不 deploy** — 等老张晚上 PT.Gril 30 天补拉时实战观察一次，看能否区分 silent vs 真稀疏。实战通过再合下次 deploy。

### 5.2 16 处间接隔离批量补刷 → 立 P2 ticket 老张主操刀

老张今早顺手 grep 全 `app/` 同 `db.query(Shop).filter(Shop.id == X.shop_id)` pattern，发现共 ~16 处全靠间接隔离（`cfg / camp / listing` 上游已带 tenant 过滤）：

- `app/api/v1/ads.py` 10 处
- `keyword_stats.py:254/371`
- `keyword_stats_task.py:175`
- `ad/service.py:1457/1626`
- `bid/time_pricing_executor.py:49`
- `product/service.py:1228/1450`

**拍板**：立 ticket `P2-tenant-defense-pass-2`，老张主操刀（文件熟、调用链熟），我 review PR。一锅 commit，不上 deploy 直接 push 下次部署捎带。目标 2026-05-09 前合。

### 5.3 v2 review 协作

老张 9 条 review 我逐条采纳写 v2，老张又逐条对账验过。这种"提议 → 采纳 → 验收"的双向 review 很顺，比单向"我说他做"高效。

### 5.4 a167196 frequency 膨胀同步部署（傍晚）

老张今天另外一记重拳：用户截图发现"搜索词洞察 vs 店级 TOP 同关键词数字差很大（耳环 9.8x / серьги 2.6x / velu 73x）"。老张定位到 c12a996（5-1 我修店级 TOP 时）漏修 `search_insights/service.py:74` —— 直接 `SUM(frequency) GROUP BY query_text` 没按 stat_date 去重，跨 (sku, day) SUM 等于膨胀 N 倍（N = 该天命中 SKU 数）。

修法跟 c12a996 同款 inner-MAX-outer-SUM 模式 LEFT JOIN。**实测 ANI-OZON 30 天三个词全部对齐**：
- velu mode: 1541 → 21（73x 偏差消失）
- серьги мед.сплава: 8224 → 3120
- серьги бижутерия 女: 5771 → 2747

**老张顺手把我 5b29b0c (clone 模块 1000+ 行) 一起 pull 部署上去** — 主动同步给我避免我看 git log 时懵一秒。这种"做了顺手事还提前同步"的协作很值钱。

**经验**：c12a996 那次我修店级 TOP 时**没去 grep 同数据源的所有聚合点**，导致 search_insights 这个姊妹场景漏修一个月（用户截图发现）。下次修 SQL 聚合 bug，必须 grep 全文找同表/同字段的所有 GROUP BY 点核一遍。

---

## 六、新业务"店铺克隆"5 个用户决策对齐过程

用户首发需求"智能跟店"很笼统，我用结构化提问 5 轮把决策抠出来：

| 决策 | 选项 | 用户拍 |
|---|---|---|
| 主菜单名 | 智能跟店 / 店铺克隆 / 跨店复刻 / 店铺克隆 / 镜像店铺 | **店铺克隆** |
| Q1: B 店来源 | A 自家另一店 / B 别人店公开 API / C 爬虫 | **A，B 也规划进来作为 Phase 2** |
| Q2: 抓后处置 | 自动推 / 待审核 + 一键批准 | **待审核** |
| 平台优先级 | 三平台一起 / Ozon 先 | **Ozon → WB → Yandex（架构留好空间）** |
| 跟踪粒度 | 只跟上新 / 上新+改价+改图+改描述 | **MVP 只跟上新 + follow_price_change 开关跟价** |
| 扫描频率 | 每小时 / 每日 / 用户拍 | **每日 + 手工执行按钮** |
| 图片处理 | A 直接 reference B 图 URL / B 下载到 OSS | **B 方案下载到 OSS** |
| 重复抓取 | A 永久跳过 / B 每次重检 | **A，但跳过明细要记日志** |

**Phase 2 现实硬限制**（提前讲明避免期待错位）：
- WB ✅ 公开 API 能列竞品全部商品 + 增量比对
- Ozon ❌ 公开 API 不能列 seller 全部商品，**Phase 2 在 Ozon 只能"用户粘 SKU 列表→批量克隆"，无法自动检测上新**
- Yandex 弱

---

## 七、遗留

### 🔴 明早 8 点跟老张联调店铺克隆 Phase 1

5 步流程（老张已确认）：
1. POST `/api/v1/clone/tasks` 创建任务（PT.Gril 当 source、shop=2 当 target，同 Ozon 平台）
2. POST `/scan-now` 触发扫描
3. GET `/pending` 看待审核列表
4. POST `/pending/{id}/approve` 批准
5. 等 5 分钟 `publish-pending` beat 跑完，到 Ozon 后台看 `offer_id=clone-xxx` 是否上架成功

### 🟡 联调可能发现的问题（按概率排序）

1. **dimensions/weight/vat 占位 → Ozon import 返错**（高概率）— 老张提议加 95014 错误码 + 前端 Modal 强制填，工作量 30 分钟。**联调跑通后再决定**：如果真撞这个 → 立即加 95014；如果用户在前端编辑 payload 时已习惯填 → 降级 warning。
2. **Ozon import_v3 attributes 字段格式不严格匹配 → 上架失败**（中概率）— 我们透传了 B 店原 attributes 但 Ozon 必填属性可能要求特定 attribute_id，需要看真错误码再补 attribute_id 兜底。
3. **AI 改写 sem=5 慢但能跑通**（低概率，老张 silent-detector 跑前可验）

### 🟢 等老张测试块（按昨天分工）

- pytest `tests/test_clone_*.py`（task_service / scan_engine / publish_engine 关键路径）
- 端到端 dryrun checklist（按 silent-detector 那种 7 信号 + 三路判定结构）
- silent-detector 晚上 PT.Gril 实战观察（跑前 ping 我对预检）

### 🟡 老张独立追加

- **tenant-defense-pass-2 P2 ticket**（18 处间接隔离批量补刷，2026-05-09 前合）
  - 类型 1+2+3 一锅 PR；类型 4 Celery 反查走我拍的 C 方案（Optional[int] 无 default + 显式 None + 双 case 全写）
- **SEO 下钻"平台搜索量"列误导**（A 案删列，老张同意，等老板拍）

### 🟢 我独立追加（等联调通过后做）

- **店铺克隆 Phase 1.1**：dimensions/weight/vat 让用户在前端 Modal 填 + 95014 错误码（如果联调撞）
- **WB / Yandex provider 真实实现**（Phase 1 stub，等 Ozon 跑稳后展开）
- **price_sync 拆出 scan_engine**（Phase 1 简化合入，Phase 2 单独频率）
- **schema_migrations 跟踪表设计**（长期架构债，老张协助）
- **WB per-seller quota counter v2 主动预测式**（长期架构债）

### 🔴 仍等用户决策（多日 P0）

- **WB 平台 API 写回方案 A/B/C**（多日，老张等）
- **DeepSeek 暴露与否**（老张 04-28 已交付对比）
- **shop=1 WB-Shario Jam 订阅**（用户决策）
- **WB 后台"顶级搜索集群" CSV 样本**（多日 P0）
- **Yandex 正确 Token**（多日 P0）
- **SEO 下钻"平台搜索量"列误导**（A/B/C 案，老张和我都倾向 A，等用户拍）

### 🔵 已知但业务正确

- shop=6 PT.Gril 真稀疏（老张 5-1 §四确认）
- shop=1 WB-Shario 没开 Jam（老张 04-28 §五.5 确认）→ wb-search-texts 必 403
- shop=5 (wb测试) status=inactive（04-24 拍）

---

## 八、明日（2026-05-03）优先级

### P0
1. **8 点跟老张端到端联调店铺克隆**（PT.Gril → shop=2 跑 1 单真实克隆，到 Ozon 后台验证 offer_id=clone-xxx）
2. **看 silent-detector 晚上 PT.Gril 实战观察结果**（如果 silent-detector 通过 → 合下次 deploy）
3. **Phase 1.1 dimensions/weight/vat 95014 决策**（联调撞了就立刻加）

### P1
4. **看老张 pytest + dryrun checklist 进度**，review 后合
5. **修联调发现的 bug**（attributes 字段 / 类目映射兜底 / 状态机三方同步等）

### P2
6. **WB / Yandex provider 真实实现**（联调通过 + Ozon 跑稳后）
7. **price_sync 拆出**（Phase 1 简化合入 scan_engine）
8. **schema_migrations 跟踪表**（长期架构债）

---

## 九、今日体会

1. **"P2 review 比 P0 实现更高 leverage"** — 今天 v1 → v2 改动 ~250 行只用了 30 分钟，但老张的 9 条 review 把所有"实现时会撞"的坑都堵了：草稿 product_id 来源 / providers 文件分工 / Phase 2 UNIQUE NULL 共存 / AI 并发限流 / reject 不可逆兜底。**实现前一次细致 review 抵 5 次 patch deploy**。

2. **"04-28 trip hook 4 天累计 — 但解法是不做"** — 老张积压 4 天的 P0 hook 终极方案，我先让他验"过去 4 天有没有真 trip"，结果 0 trip → 防护已经够用 → P0 → P2 降级。**积压不一定要做，先验证还有没有这个问题**。这是反直觉的：积压压力越大越容易"为做而做"，但反而该花时间验证前提。

3. **"silent rate-limit 是 trip 的二号物种"** — 老张 5-1 §三发现的同物种独立现象，cooldown 防护抓不到。**我让老张起 memory 记账物种特征 + 区分手段**，比把它揉进现有 detector 更有价值——这种"新物种"以后还可能撞上，每次都靠记忆复盘成本太高。**reference memory 是组织级的 grep**。

4. **"假阳性的代价是占用决策带宽"** — 我 04-28 §九 留下的"bid_management.py:91-95 缺 tenant_id"今天自查发现是误报（Explore agent 行号定错或别的）。这条假阳性占了我 4 天的"待办意识"。**Trust but verify 不是口号，agent 报告必须 grep 验过再写进遗留清单**。

5. **"3 天空档不补也行，但要在新日志里说明"** — 4-29/30 离线 + 5-1 没写一共 3 天没我的日志，今天补不补两难。补会让信息乱（事实上没做事），不补会让"我去哪了"成迷。**今天日志一行带过"git log 显示这两天范围我个人无 commit"** 比假装回溯写"今天我做了 X" 诚实得多。

6. **"5 决策结构化提问"** — 用户首发"智能跟店"需求很笼统，我没有立刻开干而是用 5 轮提问把决策抠出来（B 店来源、待审核与否、跟踪粒度、平台范围、图片方案、重复抓取）。**用户的初始需求都是模糊的，结构化提问比"你想怎么做"高效 10 倍**。Q1/Q2 是分水岭问题，Q3-7 是细节问题，按重要性排顺序问，避免用户疲劳。

7. **"一份规范文档 = 老张 + 小明 + 老板的共同接口"** — store_clone.md 14 章每章受众不同（§3 老张 / §12 小明 / §1 老板）。**好规范文档是协作合同**：老张 review 过 = 实现路径无歧义；小明读了 §12 = 前端 mock 可以开工；老板看 §1 + §0 = 业务方向能拍。比微信群里碎片 sync 高效。

8. **"用户拍'立刻开干'是合理决断 — 但工程师要把风险讲清"** — 用户下午突然说"全部实现一下"绕过老板拍 v2，我先列了两个风险（设计有问题返工 / 老张已收工）+ 4 个待拍问题（老板窗口 / 前端怎办 / 平台优先级 / 老张要不要回）。**用户拍完后我才动手**。这是 CLAUDE.md "Executing actions with care" 的本意：**承担行动后果之前，先把成本曲线画给决策者看**。如果出问题，用户知道是他选的不是我擅自的。

9. **"a167196 frequency 膨胀靠用户截图发现 — 我 c12a996 当时漏 grep 是源头"** — 5-1 我修店级 TOP frequency 时**没去 grep 全文找同表/同字段的所有 GROUP BY 点**，导致 search_insights 这个姊妹场景漏修一个月。**修 SQL 聚合 bug 的扩展自查模式**：grep `GROUP BY .*<字段>` 在所有相关 service 里扫一遍核 inner-MAX-outer-SUM 是否一致。这条加到 CLAUDE.md 自查命令里。

10. **"19 commits 一天但每步小步提交是核心"** — 今天总改动 30+ 文件，但每完成一个有意义的 unit（providers / task_service / scan_engine 各一个 commit）都立刻 push。**任何一步崩了从最近 commit 回滚最多丢 1-2 小时**。如果攒到尾部"全部完成一起 commit"，崩一次就毁灭。CLAUDE.md "小步提交"在密集开发里是真救命。

11. **"老张顺手部署 5b29b0c 是高情商协作"** — 他 a167196 deploy 时把我未部署的 clone 模块 1000+ 行一起 pull 上线 + 主动告我"clone 新模块零调用零影响但你应该想知道"。这种"顺手做的事 + 主动透明同步"避免我看 git log 时懵一秒。**协作里"做"和"告"是两件事，告通常更值钱**。

12. **"Phase 1.1 95014 老张提议但我说先不加" — 这不是拒绝是延期**。老张抓 dimensions/weight/vat 占位会被 Ozon 24h 下架的真问题，但 95014 工作量 30 分钟 + 加错地方风险存在。**真知识 = 把"现在可以加"和"应该加"分开**：联调跑通后第一个 publish 真被下架了 → 加；用户在前端 Modal 已习惯填 → 降级为 warning。**先验证前提，再决定动作**。这跟体会 #2 (trip hook) 是同源原则。

---

**老林** 2026-05-02 完整收工 🌙

**全栈实现店铺克隆 Phase 1**（19 个 commit + 2 次生产部署 + 30+ 文件改动 / 后端 ~3000 行 + 前端 ~900 行）：
- 上午（6 commit）：04-28 遗留收 4 条 + 规范 v1 → v2（老张 9 条 review）+ migration 草案
- 下午（13 commit + 2 次部署）：migration apply 生产 + 后端骨架 (providers / task_service / scan_engine / publish_engine / api / celery beat) + 前端 3 页 + 双部署上线

**今日老张同步交付**（不是我做的但要记账）：
- silent-detector commit `38c878e`（晚上 PT.Gril 实战观察）
- tenant 327/398 防御 commit `9be2e35`（已部署）
- a167196 search_insights frequency 膨胀修复（老张今日 7 修真神级）

**明早第一件事**：8 点跟老张端到端联调店铺克隆，PT.Gril → shop=2 跑 1 单真实 Ozon 克隆。
