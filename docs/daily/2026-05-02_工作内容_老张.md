# 2026-05-02 老张工作日志

## 一、今日完成 commits

| commit | 内容 | 部署 |
|---|---|---|
| `9be2e35` | ad_auto_exclude_task.py 327+398 加 `Shop.tenant_id` 纵深过滤（老林 5-2 自查派） | ✓ 已部署 |
| `38c878e` | search_insights/service.py 加 WB silent rate-limit detector — 业务层 | ⏸ 等晚上 PT.Gril 实战观察通过再 deploy |
| `00b004d` | docs(daily) 老张今日产出 + silent-detector dryrun checklist + tenant pass-2 18 处清单 | — |
| `b0fb310` | docs(daily) 类型 4 Celery 反查 C 方案落地（无 default `Optional[int]` 强制 IDE/lint 提示） | — |
| `a167196` | search_insights frequency 膨胀 N 倍修复 — 同 c12a996 漏修一处 | ✓ 已部署 |

memory 新增：
- `reference_wb_silent_rate_limit.md` — WB 第二种限流物种特征 + 与 trip 的区分 + detector 实现层取舍

## 二、修复/改进的接口（非新增 — 老张今日是 fix + review 工种）

| 接口/模块 | 改动类型 | commit |
|---|---|---|
| `GET /api/v1/search-insights/list-shop` | bug 修：frequency 膨胀（同 c12a996 漏修），LEFT JOIN 拼 inner-MAX-outer-SUM 子查询 | a167196 |
| `app/services/search_insights/service.py:refresh_shop` (WB 分支) | feat：加 silent rate-limit detector — 连续 N 批 0 行进 silent / sleep 30→120s / 当天累计 5 触发 break | 38c878e |
| `app/tasks/ad_auto_exclude_task.py:auto_exclude_keywords` + `:auto_exclude_for_campaign` | sec：Shop / AdCampaign 查询补 `tenant_id` 纵深过滤（路由层已 owned_shop 守卫，纵深双层） | 9be2e35 |

## 三、Review 给老林（远程协作）

- **store_clone v1 → v2 9 条 review**（892119d）：P1 product_id 来源 / providers 按平台分；P2 UNIQUE/扫描窗口/AI 兜底/restore；P3 *Client 不绕路。全采纳
- **migration 0638274 → e364019 4 处 review**：字段类型对齐 BIGINT UNSIGNED（最关键，避免 JOIN 索引失效）/ idx_tenant_log_type / detail JSON 4 类 log_type 注释 / ORM clone.py 规则 6 合规
- **SEO 下钻"平台搜索量"列误导 3 案 → 同推 A**（删列）
- **57cb89f 收信**：老林一口气把 store_clone 16 个 API 路由 + 2 个 Celery beat + Ozon provider 真实实现 + 完整闭环全部部署。明早 8 点联调

## 四、silent-detector dryrun 观察 checklist（晚上 PT.Gril 30 天补拉用）

跑前：
- [ ] 确认 `wb:seller_quota_exhausted:*` Redis 0 key（trip cooldown 不在生效，确保观察的是 silent）
- [ ] 确认 PT.Gril 走的是 service.py 标准路径（refresh_shop），不是临时 ad-hoc 调用

跑中观察（合并日志 + DB + task_logs）：
| 信号 | 期望（通过） | 异常需回调 |
|---|---|---|
| API HTTP code | 200 全程 | 429 → 不是 silent，是 trip |
| 单天 inner loop 行为 | 连 ≥3 批 0 行 → 进 silent 模式（log: `silent rate-limit suspected`） | 进不去 silent 模式 → 假阴性，threshold/sleep 策略要查 |
| silent 模式 sleep | 30s → 120s 切换 | sleep 没拉长 → `pause_s` 选择逻辑 bug |
| silent 期间任意批返非空 | 退出 silent + log `silent mode exited` + reset consecutive_empty | 无法退出 → 状态机锁死 |
| 当天累计 silent 触发 | ≥5 → break 当天 + errors append `silent_rate_limit_day_abort` | 累计达 5 但没 break → 退出条件 bug |
| task_logs 记录 | status='partial'（refresh_shop 返 errors → record_sync_run 走 partial 路径）| status='success' → task 层 errs 兼容路径未走通 |
| 跨天兜底 | day_total=0 + EMPTY_DAYS_ABORT=3 整天 0 行 → 整体 early exit | 跑完 31 天没退出 → EMPTY_DAYS_ABORT 失效 |

判定：
- **通过**：上表全期望命中 + DB 至少新增 ≥1 天非 0 数据
- **回调阈值**：3 → 5（threshold 太激进。判定依据：PT.Gril 已知 4-21 / 4-22 / 4-23 是真稀疏天，如果 detector 在这些天误判为 silent 进入慢速 → 改 5）
- **假阴性**：HTTP 200 + 实际全 0 行 + detector 没进 silent → 检查 `consecutive_empty` 计数路径
- **副作用**：silent 模式下 sleep 120s × 31 批 ≈ 62 min，跑完时间是平时的 2x 多；如果运行超过 90 min 视为时间预算不足，要加 max_runtime 剪枝

## 五、tenant-defense-pass-2 — 18 处目标清单（周中批改）

> 老林 ticket P2-tenant-defense-pass-2，目标 2026-05-09 前合一锅 PR，老张主操刀，老林 review。

### 类型 1: 直接漏（前端入参直入查询，零隔离）

| 文件:行 | 上游 | 修法 |
|---|---|---|
| `app/api/v1/keyword_stats.py:254` | `shop_id: int = Query(...)` 直接入参 | 加 `Shop.tenant_id == tenant_id` |
| `app/api/v1/keyword_stats.py:371` | `req.shop_id` POST body 直接入参 | 加 `Shop.tenant_id == tenant_id` |

**优先级最高** — 攻击者枚举 shop_id 可能拿到 Shop.api_key（看 response 投影）。

### 类型 2: camp/campaign 间接隔离（camp 已 tenant filter，shop 跟着查没显式过滤）

| 文件:行 | 上游 |
|---|---|
| `app/api/v1/ads.py:739` | `camp = db.query(AdCampaign).filter(id==X, tenant_id==tid)` ✓ 间接 |
| `app/api/v1/ads.py:796` | 同上 |
| `app/api/v1/ads.py:936` | 同上 |
| `app/api/v1/ads.py:1422` | 同上 |
| `app/api/v1/ads.py:1599` | 同上 |
| `app/api/v1/ads.py:1648` | 同上 |
| `app/api/v1/ads.py:1807` | 同上 |
| `app/api/v1/ads.py:1872` | 同上 |
| `app/api/v1/ads.py:2359` | 同上 |
| `app/api/v1/ads.py:2637` | 同上 |
| `app/services/ad/service.py:1457` | `campaign.shop_id` 间接 |
| `app/services/ad/service.py:1626` | 同上 |

12 处批量加 `Shop.tenant_id == tenant_id`，机械改动 ≈3 行/处。

### 类型 3: listing 间接隔离

| 文件:行 | 上游 |
|---|---|
| `app/services/product/service.py:1228` | `listing.shop_id`（listing 已带 tenant filter） |
| `app/services/product/service.py:1450` | 同上 |

### 类型 4: Celery 反查 tenant_id（特殊 pattern）

老林 5-2 拍板 **C 方案：条件加过滤 + Optional 显式 None**

落地约定（**关键：`tenant_id` 无 default 值**，让 IDE/lint 强制提示调用方显式传）：

```python
def some_helper(
    db: Session,
    tenant_id: Optional[int],   # 必填，但允许显式传 None；不写 = None 默认值
):
    """tenant_id 语义：
    - None:  Celery beat 全租户扫描（run_bid_management 等）— 规则 4 例外
    - <int>: 用户路径（CRUD / scan-now / enable / disable），
             必须传值做规则 1 纵深防御
    """
    q = db.query(Shop).filter(Shop.id == X.shop_id)
    if tenant_id is not None:
        q = q.filter(Shop.tenant_id == tenant_id)
    ...
```

调用 site 强制显式 kwarg（防位置参数眼花）：
- 用户路径：`some_helper(db, tenant_id=tenant_id)`
- Celery beat：`some_helper(db, tenant_id=None)  # beat 全扫`

**为什么不写 `Optional[int] = None`**：有 default 值的话用户路径开发者会忘记传，纵深防御就失效。无 default = IDE 必报"missing argument"，强制看注释做选择。

| 文件:行 | 来源 | 应用 C 方案 |
|---|---|---|
| `app/services/bid/time_pricing_executor.py:49` | 已有 `tenant_id: int = None` | 改成无 default 的 `Optional[int]` + 加 if 条件过滤 + 改 callers 显式传 |
| `app/tasks/keyword_stats_task.py:175` | Celery 函数参数 shop_id | 同上 pattern |

### 整体安排

- **周中（2026-05-06 / 07）**：类型 1 + 2 + 3 = 16 处批改一次 commit，commit message 列每处行号
- **跟老林对决策**：类型 4 两处单独 commit
- **不部署**：commit + push 后等下次正常 deploy 捎带（老林约定）
- **测试**：现有路由层 `get_owned_shop` 守卫 + service 层加 tenant_id 过滤 = 双层。修完跑一遍 ads/keyword-stats 的现有 e2e 测试（如果有），没的话至少手测一个跨租户枚举 case

---

## 六、用户跨页面 Q&A 备忘（不算 bug，记账）

### 6.1 SEO 优化建议-店级 TOP 与数据报表-搜索词洞察是同源吗

是。两边读同一张 `product_search_queries` 表。差别在**聚合 SQL**。

修 a167196 之前，搜索词洞察 `SUM(frequency) GROUP BY query_text` 没按 stat_date 去重 → 跨 (sku, day) 直接 SUM 等于膨胀 N 倍（N = 该天命中 SKU 数）。修后两个页面同关键词数字完全一致。

### 6.2 velu mode бижутерия 在店级 TOP 排第 1，搜索词洞察看不见

不是 bug，是**两个页面排序维度不同 + 都做截断**：
- 店级 TOP：客户端 Antd Table 默认按 **score**（`log10(订单+1)*4 + log10(曝光+1) + log10(加购+1)*2 + min(商品数,10)*0.3`）DESC，前 200
- 搜索词洞察：按平台搜索次数 DESC，前 2000

velu mode бижутерия：score=6.0 排前几（曝光 939 + 商品 108 加分凶猛）；frequency=21 排 #486 / 8691（在前 2000 内但要翻 25 页）。**业务正确**：店级 TOP 关心"对我家有价值的词"，搜索词洞察关心"市场上热搜的词"。

### 6.3 隐含潜在问题（已记账，不阻塞）

店级 TOP 的**后端 SQL 排序与前端展示排序不一致**：
- 后端 `ORDER BY revenue DESC, orders DESC` 取前 200
- 前端 Antd Table `defaultSortOrder='descend'` 按 `score` 二次排序

如果店里"score 高但 revenue=0"的词超过 200 条，后端会漏掉它们（按 revenue=0 时排序退化到 ID 自然顺序）。当前 ANI-OZON 1761 词里 revenue>0 不多，velu mode 能进 200 是因为 fallback。

修法两条路（不今天动）：
1. 后端默认 `ORDER BY` 改成 score（要把 score 算进 SQL，不是 Python 后排序）
2. 前端切 score 排序时同步刷后端 `sort=score_desc`（要后端支持这个 sort 参数）

---

## 七、遗留 bug / 技术债

| 项 | 严重度 | 何时处理 |
|---|---|---|
| 店级 TOP 后端 ORDER BY revenue 与前端 score 排序不一致（§6.3） | 🟡 中（小店暴不出，大店可能漏词） | 老板提需求或下次 SEO 模块改进时一起 |
| 店铺克隆 Phase 1.1：dimensions/weight/vat 占位 100×100×100mm/100g/0% → 上架 24h 内 Ozon 下架 | 🔴 高（联调发现真实问题就要立刻补） | 明日联调中观察，建议 approve 前必填校验 + 95014 错误码 |
| WB shop=6 PT.Gril 30 天数据补拉 | 🟢 低（业务非阻塞） | 等晚上 WB 配额恢复 + silent-detector 实战 |
| 04-29 / 04-30 老张日志缺失（8 commits 无 docs） | 🟢 低（追溯需要） | 明日补两天日志 |
| tenant-defense-pass-2 18 处批改（§五） | 🟡 中（多租户隔离纵深防御） | 周中（2026-05-06/07）一锅 PR |
| 类型 1 keyword_stats.py:254/371 是 P0（攻击者枚举 shop_id 可能拿 api_key） | 🔴 P0 | 周中清单里包含，但建议提前到明日下午做 |

---

## 八、明日（2026-05-03）计划

### 上午 8 点：店铺克隆端到端联调（老林安排）

按老林 57cb89f 给的 5 步：

1. POST `/api/v1/clone/tasks` 创建任务（source=PT.Gril Ozon / target=shop=2）
2. POST `/api/v1/clone/tasks/{id}/scan-now` 触发扫描
3. GET `/api/v1/clone/pending` 看待审队列
4. POST `/api/v1/clone/pending/{id}/approve` 批准 → 等 5 分钟 publish-pending beat 跑
5. GET `/api/v1/clone/logs` 看 publish 日志 + 到 Ozon 后台看 offer_id=clone-xxx

联调中重点检查：
- `dimensions/weight/vat` 占位是否触发 Ozon 报错（如是 → 立刻补 95014 必填校验）
- AI 改写 `sem=5` 并发是否撞 Kimi 限速
- 类目映射 `use_local_map` 跨平台流程在 same_platform=Ozon→Ozon 场景跳 028 是否走得通
- `last_check_at IS NULL` 首次扫描走 `created_at - 7d` 窗口语义是否对

通过条件：能完成一条 B→A 商品端到端上架，Ozon 后台能看到 offer_id=clone-xxx

### 上午 / 下午

- 04-29 / 04-30 老张日志补（依据 git log 反推 8 commit 内容写）
- 视联调结果决定要不要立刻动 Phase 1.1（dimensions/vat 必填校验）

### 视情况

- silent-detector 实战如晚上跑通过 → 下次 deploy 捎带（顺路），如回调阈值 → 改 commit
- tenant-defense-pass-2 类型 1（P0 两处）能提前到明日下午动手

### 不在明日范围

- providers 实现（老林 57cb89f 已完成 Ozon，WB/Yandex stub Phase 1 不做）
- scan_engine / publish_engine（老林已写完）
- store_clone Phase 2 公开 API（架构留口完成，业务后期）
