# 2026-04-14 工作内容 — 小明（前端）

---

## 一、今日完成

### 1. AI 调价 UI v2 接入（AdsAIPricing.jsx）

- 出价管理主页 `BidManagement.jsx` 接入 `AdsAIPricing` v2 组件，替换旧 AI 调价 UI（937b17e）
- AI 调价组件按 platform 分 CPM / CPC 标签（24432f9）
- WB 平台标识改为"全自动模式（CPM 商品级）"（6d51e7b）

### 2. 策略模板简化

- 策略模板简化为**只设净毛利率**，其他字段（target_roas / min_roas / max_bid / max_adjust_pct）由后端自动推算（72abc64）
- 默认净毛利率 30%，去掉系统自动推算展示块（ad2092c）
- 增加提示文案"请修改每个商品的净毛利率"（a4e2ae8）

### 3. AI 诊断概览面板

- "待确认建议"列表上方新增折叠卡片：`攒数据中 X / 仅可降价 Y / 全量决策 Z`（74c3b56）
- 展开可看 SKU 明细（天数 / 曝光 / 点击 / 花费 / 订单 / ROAS / 状态）
- `frontend/src/api/bid_management.js` 增加 `getAiDiagnostic(shopId)` 客户端

### 4. AI 分析对话框去重

- `extractSuggestions` 按 `campaign_id + platform_sku_id` 去重，解决 DeepSeek 同一 SKU 输出多版建议前端显示 3 条后端存 1 条的不一致（3343fd1）
- `sku_name` 展示改为 `商品名 · SKU_ID`（多个 SKU 共用 subject 名时可区分）

### 5. 广告 Tab 自动同步收窄

- 出价管理 / 数据分析 / 预算管理 Tab 不再自动同步（223266e）
- 自动化规则 Tab 也不再自动同步（15b5905）
- 仅"广告概览" Tab 进入时触发（>30 分钟才拉）
- 前端广告同步按钮切到新 `bid-management/data-sync` SKU 级路径（6e3a489）

### 6. 出价管理净毛利率驱动 UI 对接

- 025 迁移（`ai_pricing_margin.sql`）后端新增 margin 字段，`AdsAIPricing.jsx` 前端同步对接 CPM/CPC 输出展示（f356170）

### 7. 本地生产构建验证

- `npm run build` 通过（16.85s，主包 gzip 792.99 kB）
- Vite 提示主包 >500 kB，暂不拆，等后续功能收敛再做 manualChunks

---

## 二、今日涉及的主要前端文件

| 文件 | 说明 |
|------|------|
| `frontend/src/pages/ads/AdsAIPricing.jsx` | AI 调价 v2 主组件（CPM/CPC 分标签） |
| `frontend/src/pages/ads/BidManagement.jsx` | 出价管理主页 + AI 分析对话框 + 诊断面板 |
| `frontend/src/pages/ads/index.jsx` | 广告 Tab 容器（自动同步收窄） |
| `frontend/src/api/bid_management.js` | 出价管理 API 客户端 |

---

## 三、待联调接口（后端老林已规划，前端等上线后接入）

1. **平台最低出价展示**
   - 后端字段：`ad_groups.platform_min_bid`（待老林加迁移）
   - 前端：SKU 卡片 / 建议列表上标注"平台最低 X₽"，低于此值的建议前端置灰
2. **CPM 指标**
   - 后端：`_calc_metrics` 新增 `cpm` + `cpm_to_bid`
   - 前端：WB 场景在 SKU 明细里加一列 CPM
3. **WB 商品售价（discountedPrice）**
   - 后端：`ad_groups.current_price` 同步 WB discounts-prices API
   - 前端：SKU 卡片展示"原价 / 折后价 / 折扣力度"，辅助用户判断毛利空间
4. **AI 算法方向切换（增量 vs 绝对值推导）**
   - 未决策，等老林后端侧完成后再改前端策略模板表单

---

## 四、明天计划

| # | 任务 | 优先级 |
|---|------|--------|
| 1 | 等老林后端加完 `platform_min_bid` 后接入前端展示 + 置灰建议 | 高 |
| 2 | `AdsAIPricing.jsx` 去掉硬编码文案（抽到 i18n / 常量） | 中 |
| 3 | 主包 chunk 大小治理：路由级 `React.lazy` 拆分（目前 2.4MB 太大） | 中 |
| 4 | CPM 字段展示（WB 场景） | 中 |
| 5 | WB 折后价展示到 SKU 卡片（等 discounts 接口接入） | 低 |
| 6 | 诊断面板 SKU 明细表的分页 / 筛选（当前数据多了滚动差） | 低 |

---

## 五、今日体会

- **v2 组件替换老组件一次过**：靠 API 客户端抽象（`frontend/src/api/bid_management.js`）+ 组件 props 保持不变，替换风险小
- **策略模板简化效果立竿见影**：从 4 个字段缩到 1 个（净毛利率），用户点三下就能跑通，其他字段自动推算避免了"参数相互打架"
- **去重要放前端而不是后端**：DeepSeek 输出重复在后端兜底 vs 前端兜底都可以做，这次放前端是因为列表渲染快、不影响已存库的正确记录
- **自动同步要克制**：原来每个 Tab 切换都触发 smart_sync，429 + 504 一直背锅；收窄到只"广告概览"后体感丝滑很多

---

**小明**  2026-04-14
