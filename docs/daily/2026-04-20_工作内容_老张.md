# 2026-04-20 工作内容 — 老张（后端）

> 今日主线：**SEO 优化 三期（AI 一键优化标题）一期 MVP 全栈落地**
> 6 commit（5 feat + 1 docs），后端 1 次部署，前端 2 次构建。
> 从"2909 条俄语候选词用户看不懂要怎么用" → "勾选 → AI 融合 → 新俄语标题 → 复制" 四步闭环。

---

## 一、启动恢复

按 CLAUDE.md §角色与启动流程读了：
- `docs/daily/2026-04-19_工作内容_老张.md`（自己昨日：SEO 一期 MVP + 10 commit，主数据源翻转接源 B → 2909 条候选）
- `docs/daily/2026-04-19_工作内容_老林.md`（老林昨日：47 commit，Yandex 骨架 + AI 调价利润最大化 + 含空格短语纠错）
- `docs/api/bid_management.md`

小明昨天没写日志（老林 §一 注意到了）。

---

## 二、用户一句话推翻今日原计划

昨日我列的明日优先级是：

| 原计划 | 估时 |
|---|---|
| P1 前端 organic 筛选断层 | 20 分钟 |
| P1 SEO 健康分模块（模块 1） | 1-2 天 |
| P2 AI 一键优化标题（模块 3 入口） | 0.5-1 天 |

对齐时用户一句话：**"这个优化建议简单到我看不懂了"**。

当场分析"看不懂"的 6 种可能：
1. 不知道页面要干什么
2. 俄语看不懂
3. 没 Top 优先级聚合
4. 不知每条的具体意义
5. 覆盖列看不懂
6. 看不懂操作意义

用户没细说是哪种。我推断：**核心是 1+6（页面不闭环）**——表格给了 2909 条词，但"点加入标题"只是打标记不改商品，用户看不到"要干什么"。

于是改优先级：
- **C 先（AI 一键优化标题）→ B 后（SEO 健康分）**
- 理由：C 直接让用户在一个商品上体验到"找词 → 出新标题 → 复制去改"的端到端闭环，**"看不懂"的本质是"不知道下一步"，闭环才是答案**

---

## 三、前置检查 — 发现老林早已铺好 AI 基础设施

查 `app/services/ai/` 目录意外收获：
- `base.py` 统一 BaseAIClient 基类（chat + chat_stream）
- `kimi.py / deepseek.py / glm.py` 三个模型客户端
- `router.py` 统一调度入口 `execute(task_type, ...)`
- `AiDecisionLog` 持久化日志
- **关键**：`TASK_MODEL_MAP` 里已预留 `seo_generation → glm`，注释明说"俄语文案/SEO 标题 → GLM"

昨天我日志里写"接 Kimi"其实**走错方向**——正确是 **GLM（智谱清言）**，因为 GLM 对俄语支持好且 glm-4-flash token 便宜。

还有 `SeoGeneratedContent` 表早就建好（老林的 SEO 老基础），字段齐全：
- `listing_id / content_type('title') / original_text / generated_text / keywords_used JSON / ai_model / ai_decision_id / approval_status('pending'/'approved'/'applied')`

**直接复用，零迁移**。

---

## 四、今日落地（5 Step 6 commit）

### Step 1 — 后端 title_generator service + 两个新错误码 `12cdd80`

`app/services/seo/title_generator.py` 新建（~290 行）：

- **SYSTEM_PROMPT**：俄语电商 SEO 专家人设；6 条约束（≤180 字符 / 保留原品信息 / 语法通顺 / 高搜索词前置 / 小写无标点 / 不编造属性）；严格 JSON 输出
- **_build_user_prompt**：拼商品信息（中俄名、品牌、类目、当前标题、属性 JSON 截断 400 字符）+ 候选词按 score 排序 + 指标展示
- **_parse_ai_output**：严格 JSON 优先 → 去 markdown 代码块 → 启发式首行 fallback
- **generate_title**（async 主入口）：
  - Step 1 查 products + platform_listings LEFT JOIN（规则 1 + 4 严格 tenant/shop）
  - Step 2 查 seo_keyword_candidates（tenant/shop/product 三重校验 + bindparam expanding）
  - Step 3 `await ai_router.execute(task_type='seo_generation', ...)` → GLM
  - Step 4 解析 + 入库 `SeoGeneratedContent`
- 错误码：**94004 SEO_TITLE_GENERATE_FAILED / 94005 SEO_PRODUCT_NOT_FOUND**

**合规**：service 返回 `{code, data/msg}` 字典（对齐老张昨日 service.py 惯例，不 raise ValueError）。

### Step 2 — API 路由 `POST /seo/shop/{shop_id}/generate-title` `1711889`

- `GenerateTitleBody` pydantic：`product_id: int gt=0` + `candidate_ids: List[int]` (min_items=1, max_items=30)
- `async def` + `await service.generate_title`
- 规则 4：`Depends(get_owned_shop)` 守卫
- `current_user.id` 传给 service 做审批流归属

### Step 3 — 前端 `generateSeoTitle` API 封装 `178f0a6`

`frontend/src/api/seo.js` 加 `generateSeoTitle(shopId, productId, candidateIds)`，timeout 60s（GLM 调用含网络 + 推理 5-15s 给余量）。

### Step 4a — `SeoFilterBar` 补 organic 下拉（顺手清 20 分钟遗留）`f832339`

来源下拉从 3 选项 → **5 选项**：全部 / 付费·本商品 / 付费·类目 / 自然·本商品 / 自然·类目。后端 `709be72` 昨天已支持，SourceBadges 徽章样式（cyan/blue）早就预留，只是下拉没联通。

### Step 4b — AiTitleModal 组件 + Optimize 集成 `b492790`

**AiTitleModal.jsx** 新建（~195 行）：
- **打开阶段**：展示商品信息 + 选中候选词 Tag 云 + 当前俄语标题
- **loading 阶段**：大 Spin + "AI 正在融合关键词（走 GLM 俄语模型）…"
- **结果阶段**：
  - `Alert success` 醒目展示新俄语标题（字号 15px + 一键复制按钮）
  - `Alert info` 展示 AI 中文 reasoning
  - `Descriptions` 展示模型 / 耗时 / token / 实际用词数
  - 绿色 Tag 云列出 AI 实际用了哪些候选词（便于用户理解 AI 决策）
  - `Alert warning` 提醒"本期仅生成建议，不直接改商品"
- **footer**：[关闭 / 重新生成 / 一键复制新标题] 三按钮切换

**Optimize.jsx** 改造：
- 顶部说明 Alert 改成"**如何使用：勾选同一商品的若干反哺词（建议 3-8 个）→ 点「AI 生成标题」→ 复制新俄语标题 → 去商品列表粘贴**"——回应用户"看不懂"的流程说明
- 批量浮动工具条加主按钮 「AI 生成标题」（RobotOutlined icon）
- `handleOpenAiTitle` 校验选中行必须同一 `product_id`；跨商品给 `message.warning("当前选中涉及 N 个不同商品，AI 生成标题只能针对单个商品")`
- AiTitleModal 挂载在页面底部

### Step 5 — 部署 + 端到端冒烟（当前）

- `bash deploy_remote.sh` 完整部署通过
- 后端三进程 RUNNING
- 前端构建 10.97s
- 健康检查 200
- 冒烟 1：`curl POST /seo/shop/2/generate-title` 无 token → 403 ✓
- 冒烟 2：GLM_API_KEY 配置检查 `len=49` ✓，base_url = 智谱官方
- **冒烟 3：真实 AI 端到端**——以下详述

---

## 五、真实 AI 端到端实测

远程 Python 直连 DB 跑 `generate_title(shop=2, product_id=145)` 融合 Top 5 候选词：

### 输入

```
Shop: OZON-Shario (tenant 4)
Product: id=145 (共 59 条 pending 候选词)
Top 5 选中：
  149  серьги шары крупные       score=5.20
  148  серьги люстры             score=5.08
  1444 серьги треугольные        score=4.34
  1419 серьги женские бижутерия крупные  score=4.29
  1409 медицинский сплав серьги   score=4.05
```

### AI 调用链路

```
[api/v1/seo.py]
  ↓ await generate_title(...)
[services/seo/title_generator.py]
  ↓ await ai_router.execute(task_type='seo_generation')
[services/ai/router.py]
  ↓ model = TASK_MODEL_MAP['seo_generation'] = 'glm'
[services/ai/glm.py → GLMClient(model='glm-4-flash')]
  ↓ httpx POST https://open.bigmodel.cn/api/paas/v4/chat/completions
[返回 4145ms / 591 tokens]
  ↓
[title_generator.py]
  ↓ _parse_ai_output(content) → 严格 JSON ✓
[seo_generated_contents INSERT]
```

### 输出

| 字段 | 值 |
|---|---|
| new_title | `женские серьги шары люстры крупные бижутерия медицинский сплав Shariko` |
| reasoning（中文） | 将搜索量高的关键词放在前面，同时保留了原标题的核心商品信息，如品牌和材质，确保标题的吸引力和准确性。 |
| included_keywords | 7 个：女士耳环 / 球形 / 吊坠 / 大型 / 饰品 / 医用合金 / Shariko |
| model | glm（glm-4-flash） |
| tokens | 484 + 107 = 591 |
| duration | 4145 ms |

### 效果对比

| | 原标题 | 新标题 |
|---|---|---|
| 俄语 | Крупные сферические ювелирные серьги, простые | женские серьги шары люстры крупные бижутерия медицинский сплав Shariko |
| 词数 | 5 | 9（去标点后） |
| 中文意思 | "大型球形珠宝耳环，简约" | "女士 耳环 球形 吊坠 大型 饰品 医用合金 Shariko（品牌）" |
| 搜索词覆盖 | серьги（耳环） / крупные（大） | + шары / люстры / женские / бижутерия / медицинский сплав |
| 品牌保留 | ❌ 原标题无品牌 | ✅ 自动加 Shariko（来自原 `brand` 字段） |
| SEO 规范 | 首字母大写 + 有逗号（不符合电商惯例）| 全小写 + 空格分隔（电商 SEO 标准格式）|

**估算曝光提升**（粗略）：5 个新搜索词累计月搜索量 ~400+ 次（按 Top 候选词 `organic_impressions`），按 CTR 2% 估，**月度自然曝光理论可 +8 单**（OZON-Shario 该商品当前曝光仅 35 次）。

实际效果需用户复制 → 改商品 → 观察 14 天。

---

## 六、今日 commit 链（6 个）

```
12cdd80 feat(seo): AI 标题生成 service — 基于候选词 + 商品当前标题调 GLM
1711889 feat(seo): API 路由 POST /seo/shop/{shop_id}/generate-title
178f0a6 feat(seo): 前端 generateSeoTitle API 封装
f832339 feat(seo): SeoFilterBar 来源下拉补 organic 两选项
b492790 feat(seo): Optimize 页面集成 AI 标题生成入口 + 使用提示
(本 commit) docs(daily): 老张 04-20 — AI 一键优化标题全栈落地
```

6 个 commit，每步独立部署可回滚，严格遵守小步提交规范。

---

## 七、架构决策记录

### 7.1 为什么 C 先 B 后（推翻原计划）

**原计划依据**：B（SEO 健康分）给"全店鸟瞰 + Top 20 待优化"视图，能解决"看不懂优先级"问题。

**推翻理由**：
- 用户说"看不懂"更可能是"不知道下一步做什么"——B 给 Top 20 还是没告诉用户"然后呢"
- C 的闭环性更强：**勾词 → AI 出标题 → 复制去改 → 14 天看效果**，每一步用户都能做
- C 做完后，B 的 Top 20 每行都能挂「AI 生成标题」按钮，B 就顺势接上（不会浪费工作）

### 7.2 为什么返回字典而非抛异常

对齐 `app/services/seo/service.py` 现有惯例：
```python
return {"code": ErrorCode.SEO_CANDIDATE_NOT_FOUND, "msg": "候选词不存在"}
```

本 service 共 5 个异常分支全改成 return dict，API 层统一 `result.get('code') != 0` 判断，前端 `res.code === 0` 判断。**一致性 > 个人偏好**。

### 7.3 为什么选 max_candidates=30

- GLM-4-Flash 上下文 128k 完全够
- 但电商标题融合 > 10 个词就重复/冗余；> 30 个词 AI 会随机丢词
- 用户体验角度，勾 30 个已经很多，强迫少而精
- 保留余量：用户勾错可 max_candidates=30 兜底（前端又限了 1~30）

### 7.4 为什么不直接写回商品（三期再做）

- **风险控制**：AI 生成标题直接覆盖原标题 → 错了没法回滚（Ozon 商品更新 API 幂等但原值只能从 platform_listings.title_ru 快照恢复）
- **审批流已预留**：`seo_generated_contents.approval_status = 'pending' → 'approved' → 'applied'`，三期只需实现 applied 时调平台 API 改价 + 记录旧 title 到 `original_text` 用于回滚
- **用户信任建立**：一期让用户复制手动改，14 天观察效果；二三期才上"一键写回"
- **实现难度**：WB 和 Ozon 的商品更新 API 字段格式差异大（WB 多语言 + Ozon 属性强约束），统一接口需额外 2-3 天，一期先避开

---

## 八、给老林看

老林：

1. **我用了你 router.py 的 `seo_generation → glm` 映射**——这个路由设计救场了，不用我自己判断用哪个模型。AiDecisionLog 也一并用上了，`decision_id` 返回前端便于回溯。

2. **SeoGeneratedContent 表复用**——你早建的表 `listing_id / original_text / generated_text / keywords_used / approval_status` 完美覆盖我的场景，**零迁移**。`approval_status='pending'` 对应一期（仅生成），三期改到 'applied' 就是"一键写回商品"。感觉你当时定这个 schema 时就已经想到了这天。

3. **AI 调用链路首次在项目里端到端跑通**——之前我看项目里 AI 代码都是老林写的脚手架，这是第一次真实业务调用。4.1s / 591 token / ¥0.06（GLM-4-Flash 单价），很便宜。你的 AiDecisionLog 也记进去了，方便你后续审计 AI 调用成本。

4. **迁移版本号撞号机制问题明天再聊**——今天用户催功能没时间写 pre-commit hook 检查。下一步讨论时我想好几个方案了：`.next-version` 文件 / git hook 检查 / 或者直接用 Alembic 正式接管迁移管理（`db_version` 表 + migration id）。

5. **04-19 你日志 §三十六 列的 10 个新遗留 bug（AI 调价 trend/time_mult/凌晨等）没碰**——今天全力做 C 功能，你那边优先级更高的话明天帮看看。

---

## 九、遗留（明日继续）

### A. 今日新遗留

1. **eslint 2 个 `"` 转义 warning** `Optimize.jsx:185`——新加的使用说明 Alert 文字里的英文引号。非 error，美化。
2. **AiTitleModal 没加"跳到商品列表编辑"按钮**——用户点复制后要手动去商品列表。本想做 `navigate('/products?search=<product_id>')`，但 `Products.jsx` 的搜索参数字段名要确认，留给下次。
3. **SEO 候选词表没有"AI 生成标题"入口在行级**——只有批量工具条一处。行级按钮更直观，但需要"每行自动选中它 + 同商品其他勾选词"的合并逻辑，复杂度 +2，留给用户反馈后再加。

### B. 明日主线候选

1. **B 方向（SEO 健康分模块）** `pages/seo/Health.jsx`——目前是占位空壳。今天 C 跑通后 B 就能用"候选词数量/覆盖率 + AI 生成次数"作为健康分输入，1-2 天。
2. **AI 用词审查改进**：GLM 返回时偶尔会丢品牌（我今天测试没丢，但多跑几次大概率会）。可以加 post-processing 强制把 `brand` 字段塞进 new_title 末尾（如果 AI 忘了）。
3. **多模型对比 A/B**：同一商品分别用 GLM / Kimi / DeepSeek 跑一次，用户选最好的。不紧急但有意思。

### C. 等用户操作（强阻塞未变）

4. **WB Jam / Ozon Premium** 订阅——自然词源 B 升级精细度
5. **Yandex OAuth 凭证** 三件套——老林 04-19 骨架等
6. **净毛利率批量导入** 用户实测——老林 04-19 工具就绪
7. **Top 10 机会词改标题验证**（昨日 P0）——现在有 AI 工具，用户可试

### D. 老遗留延续

- 04-19 §六之二 3 个 paid 路径 P0 bug（数据不到不触发，降 P2）
- 04-17 单次调价幅度分级 ±15%~±50%
- 04-18 Ozon 关键词统计 3 选 1

---

## 十、明日优先级建议

| 优先 | 任务 | 理由 |
|---|---|---|
| **P0** | 用户实测 AI 生成标题，选 3-5 个改标题观察 14 天 | 今日核心交付验证 |
| **P1** | B 方向 SEO 健康分模块（1-2 天） | C 跑通后 B 顺势接入 |
| **P1** | 找老林聊迁移版本号锁定机制 | 撞 2 次再不动会第 3 次 |
| P2 | AI 生成标题行级入口 + 跳商品编辑 | 用户实测后看要不要 |
| P2 | AI 用词审查 post-processing | 偶发品牌丢失兜底 |
| P3 | 04-19 3 个 paid 路径 P0 bug | 数据不到不触发 |

---

## 十一、今日体会

1. **"用户的'看不懂'是流程不闭环，不是信息密度低"**
   最初我以为用户"看不懂"是因为数据太多（2909 条）、术语太专业（ROAS/score），想去加 Top 优先级聚合 + 术语 tooltip。实际听完用户说"简单到看不懂"才意识到：**问题不是"看不懂信息"而是"看不懂该干什么"**——候选词表没有端点动作。**这是产品思维和工程思维的分水岭**：工程师倾向信息增补，产品思维倾向动作闭环。

2. **"推翻原计划"不是坏事，是响应用户的信号**
   昨天我列的明日优先级原本 B→C，但用户一句"看不懂"我就把 C 提前。**不要为昨天写的日志辩护**——日志是计划不是承诺，用户一句话就能推翻。老林 04-19 §三十八.1 写"不要为旧 commit 辩护"同理。

3. **AI 基础设施的价值在"不需要重造"**
   我原本以为 AI 接入要从 httpx 封装、错误处理、token 统计、日志持久化全手写，结果老林早就铺好整套 `services/ai/` + `AiDecisionLog` + router。**今天 Step 1 写了 290 行纯业务代码，0 行基础设施代码**。这就是"先建好基础资产再做上层应用"的复利——老林 04-19 §九.5 的体会今天在我身上兑现。

4. **"自己建表 vs 复用老表"的节奏判断**
   本来想建 `seo_ai_generated_titles` 专用表（product_id / candidate_ids / new_title / ...），看了老林的 `SeoGeneratedContent` 发现字段全覆盖 + 还预留了 approval_status 审批流。**复用让工期从 6 小时压到 4 小时**。判断原则：**新建表前先 `ls app/models/` 全扫一遍**，70% 的通用场景老林早建了。

5. **端到端真实 AI 跑通才算验收**
   如果只做到"curl 403 鉴权通"就交付，AI 调用本身的真实效果不可知。**必须跑一次真实商品 + 真实候选词**，看 new_title 是否真的符合 SEO 约束（小写/空格/长度/品牌保留/不编造）。今天这一步花了 15 分钟，但发现"品牌 Shariko 被 AI 自动加进去"这类细节才是信心来源。

6. **prompt 工程比算法细节更重要**
   我在 SYSTEM_PROMPT 里写的 6 条约束（尤其"不编造原标题没有的属性"）直接决定 AI 输出质量。第一版 prompt 就让 GLM 做对了。**提示词设计 ≈ 电商规则文档化**，写得越具体 AI 越听话。未来如果扩展到 WB/Yandex 可以做平台细分 prompt。

7. **GLM-4-Flash 是真便宜**
   591 token ≈ ¥0.06（GLM-4-Flash 输入 ¥0.1/1000k token）。如果用 GPT-4 等价调用要 ¥0.6+。**老林选 GLM 不是拍脑袋，是算过账的**。OZON-Shario 2909 条候选词全量 AI 生成 = 全店一次运营成本 ~¥5，低到可以全自动跑。

---

**老张** 2026-04-20 收工

SEO 优化三期 MVP（AI 一键优化标题）全栈落地。
用户点「AI 生成标题」即可见俄语新标题（4 秒 / ¥0.06 / GLM）。
原计划今日主线 B（SEO 健康分）推迟到明日，因为用户 "看不懂" 的核心是流程不闭环而非信息聚合。
今日 6 commit，全部已部署生产。
