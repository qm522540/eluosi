# 2026-04-19 工作内容 — 老林

> 今日主线：恢复会话 → 发现 043 重号 + 5 个 P0 隐患 → 全部修掉 + 部署生产。
> 6 个 commit，1 次后端部署，1 次迁移（047 DROP 空表），1 次前端构建。
> 没起新功能，全是收口。等 Ozon Premium / WB Jam 订阅开通就能验收昨日交付。

---

## 一、启动恢复

按 CLAUDE.md 流程读了：
- `docs/daily/2026-04-18_工作内容_老林.md`（自己昨日 25 commit / 4 张新表）
- `docs/daily/2026-04-18_工作内容_老张.md`（搜索词洞察骨架 + 6 commit）
- `docs/api/bid_management.md`

**注意**：04-18 缺小明日志（昨天我也提了"明日对齐老张"，没提小明缺勤）。

---

## 二、生产验证 — 发现 043 重号

按例先验数据库迁移状态。`db_version` 表压根不存在 —— `deploy_remote.sh --migrate` 是裸跑 SQL 不记录版本。直接 `SHOW TABLES` 比对：

```
ad_campaign_auto_exclude     ✓ (043 老林)
ad_auto_exclude_log          ✓ (043 老林)
product_search_queries       ✓ (043 老张) ← 撞号！
ozon_product_queries         ✓ (045 老林)
ad_keyword_protected         ✓ (042 老林)
```

文件名两份 `043_*.sql`：
- `043_ad_campaign_auto_exclude.sql`（老林）
- `043_product_search_queries.sql`（老张）

生产已两张表都建好（`deploy_remote.sh --migrate` 按文件名跑哪份都行），仓库里却撞号。

---

## 三、Code Review（昨日 25+6 commit 全扫）

派 Explore 子代理深度 review，自己同步看核心冲突点。结果按规则 1/2/4 分级：

### P0（必须改，5 处）

| # | 文件 | 问题 |
|---|---|---|
| P0-1 | `app/api/v1/ads.py:1717-1765` `/auto-exclude/summary` | **跨店聚合 bug**：全租户 SUM，前端选 A 店但卡片显示 A+B+C，违反规则 4 |
| P0-2 | `app/api/v1/ads.py:1389-1488` `/ozon-sku-queries*` | 未走 `Depends(get_owned_shop)`，违反规则 4 |
| P0-3 | `app/services/search_insights/service.py:174` `list_by_product` | 只校验 `products.tenant_id`，未限 `q.shop_id`，跨店泄漏 |
| P0-4 | `app/services/search_insights/service.py:248-293` `_upsert_rows` | `INSERT ... ON DUPLICATE KEY UPDATE` 未 SET tenant_id（违反规则 1 纵深） |
| P0-5 | `app/tasks/ozon_product_queries_task.py:94-119` | SELECT-then-INSERT 而非真 upsert，并发竞态 |

### 架构级（最严重）

**老林 `ozon_product_queries` 与老张 `product_search_queries` 数据源完全相同**（同一 Ozon API `/v1/analytics/product-queries/details`）。两个 task 各跑一次会双倍消耗 Premium 配额。**应当合并到老张的 `product_search_queries`**（已是平台并集设计，加 `platform='ozon'` 即可）。

### P1 / P2

5 + 3 项，今日不动，下次会话拣。明细见 review 报告（已在主对话）。

---

## 四、修复执行（按优先级一步步做掉）

### 4.1 Task 1：合并 Ozon 双表 — `61280ba`

合并前生产确认两张表都是 0 行 → 零数据迁移成本。

- `ozon_product_queries_task.py` 写入改 `product_search_queries`，`platform='ozon'`
  - 真正的 `INSERT...ON DUPLICATE KEY UPDATE` 修原 SELECT-then-INSERT 并发竞态（顺手解 P0-5）
  - 显式 `SET tenant_id`（规则 1 纵深）
  - `date_from/date_to` 落 `extra` JSON 保留 API 窗口信息
  - 清理 90 天数据加 `platform='ozon'` 过滤（共用表不能全删）
- `/ozon-sku-queries` + `/ozon-sku-queries/sync` 改走 `Depends(get_owned_shop)`（顺手解 P0-2）
- 删 `app/models/ozon_product_query.py`（孤儿 model，未在 `__init__.py` 导出）
- 写 `047_drop_ozon_product_queries.sql`（DROP 空表）

合并完成后，下游影响：
- 前端 `/ozon-sku-queries` 接口签名/响应字段不变，前端零改动
- Celery beat schedule 仍叫 `sync_ozon_product_queries`，仅内部写入路径变了
- 等 Premium 一通，老张和我看的是同一份数据

### 4.2 Task 2：P0-1 auto-exclude/summary 加 shop_id — `3f5dd6f`

后端：
- 接口加 `Depends(get_owned_shop)`，SQL WHERE 加 `shop_id`
- JOIN `ad_campaigns` 条件加 `c.tenant_id = l.tenant_id` 兜底（防跨租户 campaign_id 碰撞，顺手清 P1#8）
- 时间用 `datetime.now(timezone.utc)`（规则 2）

前端：
- `getAutoExcludeSummary(shopId, days)` 加首参
- `KeywordStats.jsx` `useEffect` 依赖 `shopId`，未选店时清空，切店时刷新

### 4.3 Task 3+4：search_insights 跨店泄漏 + upsert tenant_id — `922a74f`

P0-3 `list_by_product`：
- 加 JOIN `products p ON p.id=q.product_id AND p.tenant_id=q.tenant_id AND p.shop_id=q.shop_id`
- 防御 `product_search_queries.shop_id` 与 `products.shop_id` 不一致时的同租户跨店泄漏

P0-4 `_upsert_rows`：
- `ON DUPLICATE KEY UPDATE` 加 `tenant_id = VALUES(tenant_id)`
- 改注释说明"哪怕 UNIQUE KEY 含 tenant_id 也按 CLAUDE.md 明文要求 SET"

### 4.4 Task 5：043 → 046 重号 — `7d29be7` + `87fd6b6`

- `git mv 043_product_search_queries.sql 046_product_search_queries.sql`
- 文件头注释改名 + 写明重号原因 + 合并历史
- 老林 042-045 是连续相关组保持不动

理由：老林那段是相关组（白名单 042 → 自动屏蔽 043 → 统一账本 044 → Ozon 045），打散注释上下文丢失；老张这张是独立模块单文件挪动成本最小。生产已建表无需重跑。

---

## 五、部署与验证

```
拉取代码: 87fd6b6
Python 语法预检: 通过
重启后端: 三进程 RUNNING (uptime 0:00:08)
健康检查: 200|{"code":0,"msg":"success",...} (第 1 次通过)
迁移 047: DROP TABLE 完成
前端构建: 3721 modules / gzip 876 kB (10.58s)
```

冒烟测三个改过的端点（无 token）：
```
auto-exclude/summary?shop_id=1   → 403 ✓
ozon-sku-queries?shop_id=2&sku=  → 403 ✓
search-insights/product/1        → 403 ✓
```

403 而非 500 = 路由加载干净 + 鉴权正常。

后端 err.log 仅有 startup OK + 老的 SAWarning（与今日改动无关，下次清）。

---

## 六、今日 commit 链（6 个）

```
61280ba refactor(ozon): 合并 ozon_product_queries → product_search_queries
3f5dd6f fix(ads): /auto-exclude/summary 加 shop_id 过滤（规则 4）
922a74f fix(search-insights): list_by_product 防跨店泄漏 + upsert SET tenant_id
7d29be7 chore(migrations): 043 重号 → 046（避开 ad_campaign_auto_exclude 冲突）
87fd6b6 docs(migrations): 046 文件头同步改名 + 注明合并历史
```

新增/删除文件：
- 新增：`047_drop_ozon_product_queries.sql`
- 删除：`app/models/ozon_product_query.py`
- 改名：`043_product_search_queries.sql` → `046_product_search_queries.sql`

---

## 七、给老张看（重要！我改了你的代码）

老张，今天 review 你的搜索词洞察发现两个隐患都在 `app/services/search_insights/service.py`，我直接改了：

**1. `list_by_product` 加了 JOIN products 防御**（line 193-204）
原查询只验 `tenant_id`，但 `product_search_queries.shop_id` 与 `products.shop_id` 不一致时会泄漏。我改成 JOIN 强制等值匹配。如果你后续要支持"跨店看同 SKU 数据"，需要单独开 endpoint 而不是去掉这个守卫。

**2. `_upsert_rows` ON DUPLICATE 加了 `tenant_id = VALUES(tenant_id)`**（line 270）
你原代码注释写"不改 tenant_id"是对的（UNIQUE KEY 已含 tenant_id），但 CLAUDE.md 规则 1 是硬规定 — 哪怕冗余也要写。我改了注释解释清楚。

**3. 我合并了我们的 Ozon 表 → 走你的 `product_search_queries`**
我那张 `ozon_product_queries`（045）废了，DROP 已跑，现在我的 task 写到你的表（`platform='ozon'`）。等 Premium 一通，你跑 `/search-insights/shop/2/refresh` 就能看到我同步的数据，同一份数据两个入口（你的洞察页 + 我的商品出价 Drawer）。
**不会双倍消耗 Premium 配额** —— 但要留意：你的 `refresh_shop` 和我的 `sync_ozon_product_queries_for_shop` 都会调 Ozon API，先后跑会拉两次。后续可以合并 task。

**4. 043 你那张表挪 046 了**
原因见上文 §四.4。生产无需重跑，仓库文件名已修。

---

## 八、遗留（明天继续）

### A. 等用户确认（强阻塞，未变）

1. **Ozon Premium 订阅**（OZON-Shario shop_id=2）— 昨天就阻塞，今天没新进展
2. **WB Jam 订阅**（WB-Shario shop_id=1）— 老张那条线阻塞
3. 订阅一通，立即跑：
   - `sync_ozon_product_queries_for_shop(2, tenant_id, 7)` 验证我合并后的写入路径
   - `/search-insights/shop/{shop}/refresh` 验证老张的 WB 路径

### B. P1（昨日 review 遗留，5 项）

| # | 位置 | 事 |
|---|---|---|
| P1#6 | `app/models/ozon_product_query.py` | 已删，moot |
| P1#7 | `app/api/v1/ads.py:1642-1678` `run_auto_exclude_now` | `apply().get()` 同步 hold worker，应改 `delay()` 返 task_id |
| P1#8 | auto-exclude/summary JOIN | 今日已顺手修 |
| P1#9 | `service.py:333,358` 平台 client 实例化不一致 | 封装统一 factory |
| P1#10 | `wb.py:734-811` set_excluded_keywords 重试用 strip().lower() 匹配 | 改用列表索引精确匹配 |

### C. P2（3 项，可优化）

- `ozon_product_queries_task.py:45,156` `date.today()` → `datetime.now(timezone.utc).date()`（合并时已顺手改）
- `service.py:344-348` SubscriptionRequiredError 短路逻辑（应 continue 后继商品而非整店 return）
- `app/models/ozon_product_query.py` `server_default=func.now()` → Python default（已 moot）

### D. 04-17 / 04-18 老遗留

- 关键词级订单归因优化（用户在考虑）
- 单次调价幅度上限按 data_days 分级 ±15%~±50%（用户同意但没实现）
- 04-18 老张提的"3 选 1：A 跳过 / B 仅 Phase 1 / C 配合标题优化反推"等用户决策

---

## 九、今日体会

1. **没有版本表 ≠ 没有冲突**：`deploy_remote.sh --migrate` 这种"裸跑 SQL 按文件名"模式，团队规模一上来就是迁移撞号事故源。今天靠"两人各自占了 043"侥幸没出大事（生产两张都建了），下一次撞号可能是 `CREATE TABLE foo (a INT)` vs `CREATE TABLE foo (b VARCHAR)`，前者跑了后者就报"table exists"，后者那张表的字段永远进不了生产。**长期看应当加版本表 + 防重复跑机制**（比如 045+ 之后挂个 hash 校验）。

2. **架构师做 review 必查"两人改同领域"**：老林+老张都触 Ozon `product-queries` API、都建了底表，只看代码 review 不看全局功能图，就会错过这种数据冗余。**昨天的"明日对齐老张"不是客套，是真的得做** — 否则今天我们就有两个 Celery task 每天都跑同一份 API。

3. **小步提交真的能让收口工作有节奏**：今天 6 个 commit，每个独立部署可回退。中间死机一次（Edit 工具拒绝 git mv 后的文件），追加 commit 修头。没有任何一次"等到全部做完才推"，每步推完心理上才能继续下一个。

4. **P0 级"跨店聚合"bug 是规则 4 的标准案例**：用户场景永远是"先选店再操作"，但开发时容易写"按 tenant 全聚合"代码。这种 bug 不是逻辑错，是产品语义错 — 单元测试根本测不出来，必须靠 review。

5. **共用底表是设计层胜利**：老张的 `product_search_queries` 平台并集设计（加 `platform` 字段 + `extra` JSON）今天直接救场，让我半小时合并完成。**先建好基础数据资产再做上层应用** —— 04-17 的 `keyword_efficiency_rules` 也是同款，4 个上层功能直接复用没新建表。这是节奏的一部分。

---

> **以下为下半场（下午 + 晚上）追加** —— 上半场原本写的"主要是收口"，下午用户开始陆续报问题，工作量比上半场翻倍。

---

## 十、用户报"商品出价加载关键词慢"——3 个 Issue 链

用户实际感知"展开关键词等十几秒"。三个并发问题层层定位、逐个修。

### 10.1 Issue 1：summary 缓存 — `b750e48`

每次展开 SKU 都调 `fetch_campaign_summary`（fullstats v3，限流 3-5 req/min）。
活动级数据所有 SKU 共享但被重复拉。

- 加 Redis 缓存 by `(advert_id, df, dt)` TTL 5 分钟
- 同活动多 SKU 切换走缓存

### 10.2 Issue 2：date_to 改昨天 — `a20cfe3`

WB 数据 1 天延迟，`date_to=today` 永远拿不到今日数据，用户看到"X/6 天"以为系统限制。
改 `date_to=today-1`，拿满 7 天有效数据。

### 10.3 Issue 3：质检结果跳页定位 — `0459554`

质检在表格第 N 页发现一个词，但表格 pageSize=20 用户在第 1 页看不到。
- 表格上方加"🔍 质检发现 N 个建议屏蔽词"信息条
- 每个 Tag hover 显示曝光/CTR/花费，click 跳到该词所在表格页
- 整行加 `.row-suggested-exclude` 浅橙色背景

### 10.4 ImportError 修复 — `6709f6e`

10.1 的 commit 用 `from app.config import settings` 但实际只有 `get_settings()` 工厂。
后端启动失败 → fix import 后重启正常。**提醒自己**：以后用 settings 前先看下别的文件怎么 import 的。

---

## 十一、用户报"WB 拒绝 1 个，应该是错误"——查日志后发现真因

`fastapi.log` 关键证据：
```
WB 报错: norm_query 'украшения' is not valid for nm 895130646
→ 自动剔除该词重试 → 剩下 43 个全部成功
```

**不是 bug，是 WB 主动拒绝**。卖饰品时屏蔽 «украшения» 被 WB 判为商品核心词。

### 11.1 顺手查 WB 文档：含空格短语其实支持

我们 04-18 写的"含空格短语全部跳过"那个兜底是**误判**。WB 文档（dev.wildberries.ru）明确：
- `POST /adv/v0/normquery/set-minus` 接受空格短语，示例就有 `"бест трикотаж"`
- 真实规则：该短语必须是 WB 系统对该 nm_id 已识别的 norm_query
- 含空格不是问题，"未识别"才是

未来想动可改"按 WB 实际反馈学习"模式。今天没动。

### 11.2 UX 改：notification 显示具体被拒词 — `175d2eb`

之前 message 只说"WB 拒绝 N 个"，今天加了 `notification.warning`（手动关）：
- 列出每个具体词为 Tag
- "为什么？" tooltip 解释 WB 拒绝两个常见原因
- import notification

---

## 十二、Tab 风格统一商品列表风格 — `e28b113` + `d5a2a81`

用户："不美观"。喜欢"商品管理→商品列表"风格。我观察 Products.jsx 后总结：
- 全白 / `#fafbff` 极淡蓝 + `#e6edff` 极淡边框
- 没有装饰性 emoji
- 字号克制，颜色克制
- 状态色集中在 Tag 上

按这个套路改 2 个 Tab：

**基本信息 Tab — `e28b113`**：
- 4 卡片去渐变（绿色 → 淡蓝），字号 30 → 22
- 流量与转化 8 格统一 `#fafbff`/`#e6edff`，数字 20 → 18
- 活动详情 Descriptions 删 emoji（📛🆔💸📅）
- 标题字号 13

**商品出价 Tab — `d5a2a81`**：
- 自动屏蔽托管卡：去 🤖 + 去绿底
- "已屏蔽 N 个"红 Tag → 默认灰
- 顶部汇总条：背景统一 + 数字 20 → 18
- 关键词展开区：灰底 → 淡蓝边
- 质检发现条：橙黄 → 极淡红边白底，去 🔍

---

## 十三、用户问"广告组什么意思" → WB 隐藏 — `b76ece1`

- 商品出价 Tab 顶部汇总条「已绑定广告组 0/N」对 WB 永远 0（WB 没有"组"概念）
- 活动详情有"广告组" Tab，对 WB 用户毫无意义

WB 时：
- 汇总条改 3 格（商品数 / 搜索均价 / 推荐均价）
- 整个"广告组" Tab 不渲染（spread + 三元过滤）

Ozon 保持原样。

---

## 十四、用户报"加载关键词慢"二次定位 → `fetch_excluded_keywords` 才是大头 — `084c777`

10.1 加了 summary 缓存后用户仍觉得慢。再看日志，发现真凶：
```
fetch_excluded_keywords 单次 19-23 秒
```

WB 的 `POST /adv/v0/normquery/get-minus` 接口本身就慢。屏蔽词列表只在用户屏蔽 / 自动 task 跑后才变 → 写后失效模型最合适。

- 加 `_get_cached_excluded` / `_set_cached_excluded` / `_invalidate_excluded`
  cache key = `wb:excl:{advert_id}:{nm_id}`, TTL 5 分钟
- `/campaign-keywords` 读时优先 cache
- `exclude_keywords` 接口写完 set-minus 后调 `_invalidate_excluded`
- `ad_auto_exclude_task` 同样在 set-minus 成功后失效

预期：同一 SKU 5 分钟内反复展开秒出，新 SKU 首次仍要 ~20s。

---

## 十五、用户报"Yandex 商品列表没数据" → 整套骨架补完

诊断：**不是 bug，是 Yandex 同步功能根本没写**（04-18 老张就阻塞，等用户 OAuth token）。

### 15.1 验证 OAuth token

ping Yandex `/v2/campaigns` → 403 `OAuth token is invalid`。token 失效是已知问题。

### 15.2 迁移 049 + ORM — `17ab6f1`

- shops 加 `yandex_business_id` + `yandex_campaign_id` 两字段
  （YandexClient.fetch_products 强依赖 business_id）

> ⚠️ **第二次撞号事件**：今早老张 11:15 推了 048_seo_keyword_candidates，我下午没拉 git log 又占了 048。和 04-18 的 043 撞号一模一样。生产两边都跑了，文件名收尾改名为 049 — `fff2328`。

### 15.3 service 层 + 同步函数 — `933574e`

- 写 `_sync_yandex_products`：仿 `_sync_ozon_products` 结构
- `sync_products_from_platform` 加 yandex 分支
- 走 business 路径 `/businesses/{id}/offer-mappings`
- shop 缺 `yandex_business_id` 立即报错（不进 Celery）
- OAuth 401/403 给友好提示
- 复用 `_archive_missing` 自动归档

### 15.4 前端店铺表单 — `f9b08bb`

- Yandex 平台时显示 OAuth Token + Business ID + Campaign ID 三块
  + 友好的获取路径说明
- handleEdit 把 client_id / perf_client_id / yandex_* 一起回填表单
  （之前 client_id / perf_client_id 编辑时也丢值，顺手修了）

---

## 十六、用户报"我更新了，前端没提示" → 同步前置校验 — `f60c2c1`

诊断结果两个问题叠加：

1. **用户的"更新"实际没发到后端** — DB 里 shops/4 仍是 NULL，所有 shops/4 请求历史里 0 次 PUT。可能是浏览器缓存住旧 JS，或没点保存按钮。
2. **同步失败前端确实不显示** — `/products/sync` 用 Celery delay 异步执行，worker 内部 `code != 0` 前端只看 `res.data.syncing == true` 就以为成功。

修：
- `/products/sync` 加同步前置校验：shop 存在 + api_key 齐 + 平台特定字段
  （Ozon 需 client_id, Yandex 需 yandex_business_id）缺则立即返 10002 + msg
- 前端 `handleSync` catch 块 `message.error(e.message)` 透传后端 msg
  （request 拦截器把 code != 0 包成 Error，e.message = msg）

---

## 十七、用户问"关键词统计 Ozon 没数据吗"

确认：**Ozon 在关键词统计页当前完全没数据**。

- `keyword_stats_task.py:115` 只 `if shop.platform == "wb"`
- 生产 `keyword_daily_stats` 表只有 wb 8505 行
- 跟 04-18 §B 留的 3 选 1 决策（A 跳过 / B 仅同步 / C 配合标题优化反推）一致 — 用户当时说"等一下后面聊"，今天又问了一次没下结论

提醒用户：等 Ozon Premium 一通先看「商品出价 Drawer 展开 Ozon SKU 显示搜索词」（昨天 Phase A 做的另一条线），可能就不需要在关键词统计页里复刻一份。

---

## 十八、今日完整 commit 链（21 个）

```
# 上半场（review + 多租户修复 + 合并 + 重号）
61280ba refactor(ozon): 合并 ozon_product_queries → product_search_queries
3f5dd6f fix(ads): /auto-exclude/summary 加 shop_id 过滤（规则 4）
922a74f fix(search-insights): list_by_product 防跨店泄漏 + upsert SET tenant_id
7d29be7 chore(migrations): 043 重号 → 046（避开 ad_campaign_auto_exclude 冲突）
87fd6b6 docs(migrations): 046 文件头同步改名 + 注明合并历史
b6a9f15 docs(daily): 老林 04-19 上半场（修 5 个 P0 + 合并 Ozon 双表）

# 下半场（用户陆续报问题，逐个修）
0459554 feat(ads): 关键词质检结果加快速列表条 + 跳页定位 + 整行高亮
b750e48 perf(ads): /campaign-keywords 缓存 fullstats v3 summary 5 分钟
a20cfe3 fix(ads): /campaign-keywords date_to 改为昨天避开 WB 1 天延迟
6709f6e fix(ads): 修上一 commit ImportError — settings 应通过 get_settings() 取
175d2eb feat(ads): 一键屏蔽结果加 notification 列出具体 WB 拒绝词
e28b113 style(ads): 基本信息 Tab 统一商品列表风格
d5a2a81 style(ads): 商品出价 Tab 统一商品列表风格
084c777 perf(ads): 缓存 fetch_excluded_keywords + 写后失效
b76ece1 fix(ads): WB 活动详情隐藏「广告组」概念

# Yandex 商品同步骨架（4 个 commit）
17ab6f1 feat(shops): 加 yandex_business_id + yandex_campaign_id 字段（撞号 048）
933574e feat(yandex): _sync_yandex_products + sync_products_from_platform 加分支
f9b08bb feat(shops): 店铺设置加 Yandex business_id + campaign_id 字段
f60c2c1 fix(products): /sync 加同步前置校验 + 前端透传错误信息

# 收尾
fff2328 chore(migrations): 048 重号 → 049（避开老张 SEO 048 撞号）
```

新增/重命名的迁移：
- 046_product_search_queries.sql（上半场，老张昨天 043 重号）
- 047_drop_ozon_product_queries.sql（上半场）
- 049_shops_yandex_business_campaign.sql（下半场，原 048 跟老张撞号重号）

---

## 十九、遗留（明日 / 后续）

### A. 等用户操作（强阻塞）

1. **Yandex 凭证三件套**（明天周一才能搞）
   - 新 OAuth Token（partner.market.yandex.ru → 个人中心 → API access）
   - Business ID（右上账户菜单 → ID кабинета）
   - Campaign ID（推广 → 广告活动 → URL 末尾数字）
   - 拿到后店铺设置填进去 → 商品列表点同步即可端到端跑通
2. **Ozon Premium 订阅**（昨天就阻塞，仍未确认）
3. **WB Jam 订阅**（老张那条线，仍未确认）

### B. 待用户决策

4. **Ozon 关键词统计** — 04-18 提的 3 选 1 仍未下结论（A 跳过 / B 仅同步 / C 配合标题优化反推）。建议先等 Premium 通了看「商品出价 Drawer × 搜索词」再定 Ozon 关键词统计页要不要做
5. **关键词级订单归因优化**（04-17 老遗留）
6. **单次调价幅度上限按 data_days 分级 ±15%~±50%**（04-17 老遗留）

### C. P1 / P2（昨天 review 遗留）

7. `app/api/v1/ads.py:1642-1678` `run_auto_exclude_now` `apply().get()` 同步执行 → 改 `delay()`
8. `wb.py:734-811` set_excluded_keywords 重试用 strip().lower() 匹配 → 改用列表索引精确匹配
9. `service.py:344-348` SubscriptionRequiredError 整店 return → 应 continue 后继商品

### D. 今天发现的新问题（已记日志，待动）

10. **WB "含空格短语跳过"是误判** — 04-18 写的兜底过度激进，WB 文档实际支持空格短语，需 norm_query 已识别。可改"按 WB 实际反馈学习"模式而不是前端预过滤
11. **Yandex 商品同步 token 模式分页未实现** — `_sync_yandex_products` 现在只支持 page-based 分页，遇到超大店铺（用 nextPageToken 模式）会提前 break。等用户拿到 token 跑一次看实际响应再决定是否要补

### E. 流程教训

12. **048 又撞号** — 04-18 是 043，今天是 048，第二次了。需要建立**版本号锁定机制**：建议加一个 `database/migrations/versions/.next-version` 文件，每次新建迁移先 +1，commit 一起推。或加 pre-commit hook 检查重号

---

## 二十、明日优先级建议

| 优先 | 任务 | 理由 |
|---|---|---|
| P0 | 用户拿到 Yandex 凭证 → 端到端验证 | 今天交付的 Yandex 骨架就等这一步 |
| P0 | Premium / Jam 订阅状态 | 阻塞昨天交付 + 老张的搜索词洞察 |
| P1 | 找老张协调"迁移版本号撞号"机制 | 两次撞号同根因，再不动会第 3 次 |
| P1 | 04-18 review 遗留 P1 仨（run_auto_exclude_now / set_excluded 精确匹配 / refresh_shop continue） | 都是小修补，明早一两小时清完 |
| P2 | 等用户对 Ozon 关键词统计 3 选 1 决策 | 不决策这块持续模糊 |
| P2 | WB 短语处理改"按反馈学习"模式 | 体感优化，不阻塞 |

---

## 二十一、今日体会（下半场补）

1. **用户体验侧的"加载慢"往往不是单一瓶颈** — 今天 fetch_campaign_keywords 表面只有 1.5s，summary 缓存解了重复调用，但真正瓶颈是 `fetch_excluded_keywords` 19-23s。先看日志找具体的接口耗时，别凭直觉定位。

2. **"我更新了"和"PUT 请求到了后端"是两回事** — 用户报问题时第一步永远是看 access log。今天用户说"我更新了 yandex 配置"但 PUT /shops/4 请求历史 0 次 — 直接说明问题不在后端，省掉一堆排查。

3. **异步任务的失败一定要有同步反馈通道** — `/products/sync` 当前模式是"接口返 200 + Celery 异步跑 + worker 失败前端不知"，用户体验上完全不可接受。最稳的是"前置校验同步报错 + 真正网络/数据问题才异步"。这个模式以后所有异步接口都该套用。

4. **风格统一是个隐性产品需求** — 用户说"不美观"，听起来主观，但我观察一遍代码就找到了 5 个具体差异（emoji / 渐变 / 字号 / 边框色 / 颜色多样性）。不要被"主观"标签吓到，往往用户能用的具体词背后都有结构性的"东西"。

5. **再一次撞号 = 流程问题不是个人问题** — 04-18 是 043，今天是 048。两次都是"两人同日做不同模块各自占了同一个版本号"。这是流程层面缺机制。不能靠个人记得 git pull 后再编号 — 需要硬约束。明天找老张商量。

6. **WB API 文档比 commit 历史里推测的更准** — 我们 04-18 commit 写"WB 不支持空格短语"是从错误信息倒推的，结果今天查文档发现是误判。每次踩 WB 坑，建议立刻去 dev.wildberries.ru 验证文档，再决定怎么处理。倒推容易把 1% 的特殊情况当 100% 的通用规则。

---

**老林** 2026-04-19 收工

全天 21 个 commit，跨 7 个领域（多租户隔离 / 合并 / 性能 / UX / 风格 / Yandex 骨架 / 流程问题）。明日重点：等用户拿到 Yandex 凭证 + 跟老张商量迁移版本号机制，避免第三次撞号。
