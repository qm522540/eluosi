# 2026-04-28 工作内容 — 老张（后端）

> 今日主线：**自查 2 日代码 review 修 4 bug + AI 描述跨品类词照搬标题侧 + GLM/DeepSeek 对比实测 + WB 搜索词同步改"按天补缺失"统一两平台口径**

---

## 一、启动恢复

按 CLAUDE.md 流程读了：
- `docs/daily/2026-04-27_工作内容_老张.md`（昨日 Ozon 写回闭环 + AI 标题 4 层防御 + 健康分 3 bug）
- `docs/api/bid_management.md`

主线 5 段：
1. 用户：「review 你这 2 天的代码」 → 自查 2 日 +3711/-1051 行 28 文件，找出 4 个 bug 修了
2. AI 描述模态：跨品类词 / 重复词标记，跟标题侧逻辑统一
3. 用户问「加 DeepSeek 改动大吗」→ 不动 UI/后端做 2 商品对比实测
4. 用户问「同步是同步多少天」→ 答错 7 天，复核发现是按 datePreset 切，**默认 30 天**
5. 用户问「WB 不能按天拉吗，跟 Ozon 一样」→ 实测确认 WB API 接受单日窗口，把 WB 同步重构为"按天补缺失"模式

---

## 二、自查 2 日代码 review（早上）

### 2.1 自查命令 + Explore agent 深度审 + 自己核验关键文件

跑了 CLAUDE.md 的自查 grep（datetime.utcnow / SQL NOW() / shop_id WHERE 计数 / tenant_id 计数）—— **规则 1/2/3/4/6 没有违规漏报**。

Explore agent 给了 7 个 issue，自己逐个核验，确认 **4 个真 bug**（已修并部署，commit `0d8f678`）：

### 2.2 致命 bug：`generated_history.py` 用 logger 但没 import

```python
# line 250
except Exception as e:
    logger.error(...)  # 但顶部从来没 from app.utils.logger import logger
```

**触发场景**：Ozon API 异常路径走到 except 分支 → `NameError: name 'logger' is not defined` → 用户看到 500 而不是友好错误信息。

昨天 `mark_title_applied` 改 async + 平台写回时落下的，except 分支没被自动测试覆盖到，**今天复盘才发现**。

### 2.3 `datetime.now(timezone.utc)` 写 DB 违反规则 6

```python
# line 197 (旧)
now_utc = datetime.now(timezone.utc)
```

**问题**：aware datetime 写 naive `DateTime` 列，与 CLAUDE.md 规则 6 字面对齐失败（"DB 写入唯一入口 `utc_now_naive()`"）。

修：`now_utc = utc_now_naive()`，from `app.utils.moscow_time`。

### 2.4 offer_id fallback 错（高风险）

```python
# 旧
offer_id = (offer_row.sku if offer_row else None) or row.platform_product_id
```

**问题**：sku 缺失时 fallback 到 `platform_product_id`（Ozon 内部 numeric ID），但 Ozon `attributes/update` API 需要 `offer_id`（卖家 SKU）—— **错配会调到错商品**。

修：fail safe，`offer_id = (offer_row.sku if offer_row else None) or None`，None 时上层走 skipped 提示，不会调错商品。

### 2.5 `ozon.update_product_attribute` 截断无告警

```python
# 旧
"values": [{"value": value[:max_chars]}]  # 静默截断，前端无感
```

**问题**：用户提交 800 字描述（max=500）会被截到 500，前端显示"已提交"以为全量同步成功。

修：加 `logger.warning("Ozon attr={} value 超长被截断 {} → {} 字符")` 让超长可被审计。

### 2.6 体会

最大教训：**异常分支不被自动测试覆盖时，里面的依赖（如 logger）极易漏 import**。考虑给 `tools/` 加个 lint：service 文件用了 `logger.` 但没 import 直接报错。

---

## 三、AI 描述跨品类词 + 重复词默认不勾（早上）

用户：「AI优化描述-同类目热门关键词如果有的关键词，那在本商品热门关键词里面就不要打勾了，没必要传给 AI 两个一样的关键词，和刚刚 AI 优化标题一样」

把昨天 AI 标题侧已有的两个规则照搬到 AI 描述侧（commit `1760747`）：

1. **同类目热门关键词** 含跨品类词（耳环商品出现 кольцо/колье/брошь 等）→ 红色 `跨品类?` Tag + 默认不勾
2. **本商品热门关键词** 已在同类目热门里 → 灰色 `同类目已有` Tag + 默认不勾

后端复用 `title_generator._detect_category_key` + `_is_cross_category`，给 cat_kws 加 `looks_cross_category`，prod_kws 加 `dup_with_cat_top`，不写新逻辑。

`_collect_inputs` 多透出 `platform_category_name` 给检测用。

---

## 四、GLM vs DeepSeek 对比实测（中午）

用户：「我想加 DeepSeek 改动大吗」→ 答 2-3h（DeepSeek 客户端早就在 `app/services/ai/deepseek.py`，router 也已经在用，只 SEO 任务硬编码 `glm`）→ 用户：「拿 2 个商品生成给我看看，UI 后端都不改」

写了独立测试脚本，**完全相同**的 SYSTEM_PROMPT / user_prompt / temperature=0.5 / 5 个候选词，对比 2 个商品：

### 4.1 商品 1：OZON-E0149（耳环）

|模型|输出（post-check 后）|字符|耗时|
|---|---|---|---|
|GLM-4-flash|`серьги из медицинского сплава большие коричневые pt girl`|56|4.9s|
|DeepSeek|`серьги большие из медицинского сплава коричневые pt girl`|56|2.9s|

两者都被后处理剔了"кольца"（耳环类目下的窜词），**质量基本一致**。

### 4.2 商品 2：OZON-N0002（项链）

|模型|输出|字符|耗时|
|---|---|---|---|
|GLM|`женская бижутерия на шею ожерелье подвеска с жемчугом колье на`|62|7.7s|
|DeepSeek|`ожерелье на шею женская бижутерия цепочка жемчуг подвеска с жемчугом кулон с колье на pt girl`|93|3.8s|

DeepSeek **词汇覆盖更全**（4 个项链同义词都进了），但**重复堆叠更明显**，post-check 后还有"кулон с"残段。

### 4.3 结论

| 维度 | GLM-4-flash | DeepSeek-chat |
|---|---|---|
| 俄语流畅度 | ✅ 略胜 | 接近 |
| 关键词覆盖 | 紧凑但偶有遗漏 | ✅ 更全 |
| 重复堆叠 | ✅ 更克制 | 偏多（依赖 dedupe 兜底）|
| 速度 | ~5-8s | ✅ ~3-4s（**快 2x**）|
| 成本 | ✅ glm-4-flash **免费** | 输出 ¥8/M tokens |

**结论给老板**：GLM 出来"完成度"更高开箱即用；DeepSeek 更快但要更厚的后处理保护层。**GLM 是免费 + 略稳，DeepSeek 没有压倒性优势**。是否暴露给用户**等老板拍**。

---

## 五、WB 搜索词同步改"按天补缺失"模式（下午）

用户准备测试搜索词洞察的 WB 同步，发现两个问题：

### 5.1 搞清楚"同步是同步几天"

我**先答错**为"锁死 7 天"——实际复核：
```js
// SearchInsights.jsx:89
await refreshShopSearchInsights(shopId, datePreset === '30d' ? 30 : 7)
```

页面顶部有"近 7 天 / 近 30 天"下拉，**默认 30 天**。我之前看 `api/search_insights.js` 函数签名 `days = 7` 默认值就以为锁死了，没看调用方实际传值。

### 5.2 用户：「WB 也能按天拉吧，和 Ozon 当时一样」

实测验证：WB `POST /api/v2/search-report/product/search-texts` 的 payload 是 `currentPeriod: {start, end}`，跨度 ≤31 天，**接受 start=end 单日窗口**。

代码里那段注释（"WB 仍 N 天聚合，API 限流没法按天拉，WB quota 恢复后再迁移"）写得偷懒——**不是不能拉，是怕烧光 quota**。

### 5.3 用户拍：「能实现就改，千万别重复」

shop=6 现有数据：
- 2 个 stat_date：4-22（85 行）+ 4-26（252 行）
- 都是 7 天聚合快照，跨日 SUM **会重叠**（4-15~4-22 双计）

### 5.4 实施（commit `37a3ed0`）

`refresh_shop` WB 分支重写为镜像 Ozon 的"按天补缺失"模式：

```python
# 1. 算窗口 [start_date, end_date]，end_date = today-2 (WB 没保护期)
# 2. 查 DB 已有 stat_date
existing_dates = {...}
# 3. force=True 清窗口内已有重拉，否则只补缺失
# 4. 计算 missing_dates
# 5. 按天循环 + 内层 nm_id 分批 (5 批 × 22s sleep)
for d in missing_dates:
    for batch in nm_id_batches:
        items = await wb.fetch_product_search_texts(
            nm_ids=batch,
            date_from=d, date_to=d,  # 单日窗口!
        )
        upsert(stat_date=d, ...)
```

**保留**：quota pre-check / WBSellerQuotaExhausted 中途熔断 / EMPTY_BATCH_ABORT 跨天累计早退。

**删**：原 WB 专用的 `_snapshot_exists` 快速 return 函数（按天模式不适用，孤儿清理 - 规则 3）。

### 5.5 数据迁移

旧 7 天聚合数据跟新按天数据**口径完全不同**（4M vs ~570K/天），混用会让跨日 SUM 数字乱。

直接删 shop=6 全部 337 行旧数据：
```sql
DELETE FROM product_search_queries WHERE shop_id=6 AND tenant_id=4 AND platform='wb';
-- 已删 337 行，shop=6 剩余 0 行
```

shop=1（WB-Shario）今早测试时**新发现**没开 Jam 订阅（403 "Available only in a Jam subscription"），memory 里"待确认"那条**今天确认**了。

### 5.6 成本

| 场景 | 调用次数 | 耗时 |
|---|---|---|
| 首次 30 天 | 100 SKU/20 = 5 批 × 30 天 = 150 次 | ~55 分钟 |
| 首次 7 天 | 35 次 | ~13 分钟 |
| 次日点 | 1 缺失天 × 5 批 | ~2 分钟 |
| 全有 | 0 次 | 秒返 |

⚠ **首次会很烧 quota**，建议测试时先选"近 7 天"验证 13 分钟跑完，再扩 30 天。

---

## 六、今日完成的接口 / 函数清单

### SEO Apply 真闭环（review 修 bug）

| 函数 / 改动 | 动作 | commit |
|---|---|---|
| `generated_history.py` 顶部 | **新增** import logger + utc_now_naive | `0d8f678` |
| `mark_title_applied` | **改** datetime.now(timezone.utc) → utc_now_naive() | `0d8f678` |
| `mark_title_applied` | **改** offer_id fallback 不再用 platform_product_id | `0d8f678` |
| `generated_history.py` SQL | **删** 死 LEFT JOIN pl2 | `0d8f678` |
| `OzonClient.update_product_attribute` | **加** value 超长截断的 logger.warning | `0d8f678` |

### AI 描述跨品类 + 重复词

| 函数 / 改动 | 动作 | commit |
|---|---|---|
| `description_generator._collect_inputs` | **改** 透出 `platform_category_name` | `1760747` |
| `description_generator.preview_description_inputs` | **改** 复用 title_generator `_detect_category_key` + `_is_cross_category` 标记 cat_kws | `1760747` |
| `description_generator.preview_description_inputs` | **新** prod_kws 加 `dup_with_cat_top` 标记 | `1760747` |
| `AiDescriptionModal.jsx` 默认勾选 | **改** filter 跳过 looks_cross_category + dup_with_cat_top | `1760747` |
| `AiDescriptionModal.jsx` 渲染 | **新** 红色 `跨品类?` Tag + 灰色 `同类目已有` Tag | `1760747` |

### 搜索词洞察 WB 按天补缺失

| 函数 / 改动 | 动作 | commit |
|---|---|---|
| `refresh_shop` WB 分支 | **重写** 按天补缺失模式（参照 Ozon）| `37a3ed0` |
| `refresh_shop` 顶部预检 | **删** WB 专用 _snapshot_exists 快速 return | `37a3ed0` |
| `_snapshot_exists` 函数 | **删** 孤儿，唯一调用点已删（规则 3）| `37a3ed0` |
| `refresh_shop` docstring | **更新** 反映 WB/Ozon 双平台统一为按天补缺失 | `37a3ed0` |

### 数据迁移

| 操作 | 状态 |
|---|---|
| `DELETE FROM product_search_queries WHERE shop_id=6 AND platform='wb'` | ✅ 删 337 行 |

---

## 七、遗留

### 🔴 强阻塞等用户决策

- **WB 平台 API 标题/描述写回方案**（A 立刻做 / B 留 TODO / C dry-run）— 用户尚未拍（昨日遗留延续）
- **DeepSeek 是否暴露给用户选**（GLM 略胜，DeepSeek 速度 2x 但贵）— 已交付对比数据，等用户拍
- **数据源管理 Tab 粒度方案**（昨日交付的方案稿）— 没动，等用户拍
- **WB seller quota 静默期实测**（多日 P0，老林主导）
- **Yandex 正确 Token**（多日 P0）
- **WB 后台"顶级搜索集群" CSV 样本**（多日 P0）

### 🟡 等用户验证

- **WB shop=6 按天补缺失实测**：现已部署 + 旧数据已清，等 quota 冷却完（今天 ~01:30 还剩 8 分钟），用户在前端选"近 7 天"点同步，预期 ~13 分钟落 7 个独立 stat_date，每个 ~570K freq 而不是 4M 聚合
- **昨日 SEO 健康分修 3 bug 实测反馈**：SS-E0349 / SS-E0383 短标题 + 无候选商品，分数应该从 20 → 80+
- **昨日 Ozon Apply 描述/标题写回平台实测结果**

### 🟢 代码层技术债

- **WB Apply 写回**：当前 mark_*_applied 对 WB skip 返 msg，要走 `/content/v2/cards/update` 全卡更新（昨日遗留）
- **公共翻译 hook 抽取**（多日遗留）
- **`app/tasks/` datetime 漏扫 follow-up**（老林主导）
- **AI 调价并行 UI 统一**（多日）
- **service 文件 logger 漏 import 类似坑**：考虑加 lint 规则，service 用了 `logger.` 没 import 直接报错（今天 review 暴露）

### 🔵 已知但业务正确

- **shop=1 WB-Shario 没开 Jam 订阅**（今天新发现，原 memory "待确认"）— 用户决策是否开通
- **shop=6 WB-Pt.Gril seller quota 一调就烧**：跟 quota 静默期相关，老林 P0

---

## 八、明天（2026-04-29）优先级

### P0

1. **看 WB shop=6 按天补缺失实测结果**（用户跑完 7 天后）— 验证 7 个独立 stat_date 数字合理 + 跨日 SUM 不双计
2. **WB 平台 API 写回方案 A/B/C 拍板**
3. **DeepSeek 暴露/不暴露 拍板**

### P1

4. **如果 WB 实测顺利** → 可以把按天模式扩到 30 天验证
5. **数据源管理 Tab 实施**（如果用户拍方案）
6. **公共翻译 hook 抽取**（多日遗留）

### P2

7. **AI 标题/描述用 5 个不同品类实战测试**（验证 4 层防御）
8. **WB-Pt.Gril Jam 订阅恢复后**：测多源合并

---

## 九、今日体会

1. **"自查代码必须真自己读，不能只 grep"**
   `generated_history.py` 漏 import logger 这个致命 bug，CLAUDE.md 自查 grep 完全检测不到——它检的是"用了 datetime.now() 没"，检不到"用了 logger 没 import"。靠 Explore agent + 自己核验关键文件才挖出来。**自查 grep 是底线，不是上限**。

2. **"答用户前先看代码不要凭印象"**
   今天答错"同步锁死 7 天"——其实页面有 datePreset 下拉默认 30 天。我看到 `api/search_insights.js` 函数签名 `days = 7` 就以为是固定值，没看调用方传啥。**API 层默认值 ≠ 实际调用值**，要看调用 site。

3. **"不能因为难就说不能做"**
   WB 按天拉这事，原代码注释写"WB 仍 N 天聚合，API 限流没法按天拉，WB quota 恢复后再迁移"——但实际是**怕烧 quota，不是 API 不支持**。用户一问"能不能跟 Ozon 一样"，我才回头实测 payload，发现完全可以。**注释偷懒会把"暂时不做的工程决策"写成"客观限制"**，下次踩坑。

4. **"GLM/DeepSeek 对比这种问题，最快答案是直接跑两边看"**
   用户问"加 DeepSeek 改动大吗"，我先答 2-3h；用户立刻反问"拿 2 商品看看"——直接对比比讲半天理论强 100 倍。**做 AI 类决策时，不动主代码做对比测试是最低成本验证**。

5. **"删数据前确认归属，规则 1 的反向应用"**
   今天删 shop=6 旧 WB 聚合数据，先 SELECT 看 stat_date / 行数，确认**只有 shop=6 + tenant=4 + platform='wb'** 三重过滤，再 DELETE。CLAUDE.md 规则 1 (tenant_id) 和规则 4 (shop_id) 不只用在 SELECT/INSERT，**DELETE 更要严格**。

---

**老张** 2026-04-28 完整收工 🌙

3 commit + 1 review 报告 + 1 段 GLM/DeepSeek 对比 + 1 个搜索词同步统一两平台口径 + 0 部署事故。

明早第一件事：看用户对 WB 按天补缺失的实测反馈 + 等 WB 平台写回 / DeepSeek 暴露 两个方案拍板。
