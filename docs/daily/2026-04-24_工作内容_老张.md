# 2026-04-24 工作内容 — 老张（后端）

> 今日主线：**用户报 Ozon-Pt.Gril SEO 优化建议无数据 → 三层诊断 + hint 文案分诊 v2 + 顺手扫尾老林漏修的 generate_daily_report 时区 + 修真 bug：fetch_xxx 吞 quota 异常**
> 上午 5 commit + 1 段日志 + 给老林同步消息（用户转发）。

---

## 一、启动恢复

按 CLAUDE.md 流程读了：
- `docs/daily/2026-04-23_工作内容_老张.md`（自己昨日 13 commit + 4 段日志 — AI 调价纵深 / 053 索引清理 / WB 脏词黑名单 / 搜索词洞察上线 + quota 事故 / SEO 候选池 not_ready 提示）
- `docs/daily/2026-04-23_工作内容_老林.md`（老林 18 commit + 4 段日志 — 时区大改造 cutover / 用户实测 7 连 bug / WB seller quota 熔断 + Redis 单例 + 查询 API / 规则 2 字面统一）
- `docs/api/bid_management.md`

---

## 二、用户报「OZON-pt-gril 没数据」— 三层诊断

### 2.1 现象

用户在 SEO 管理-优化建议页面选 OZON-Pt.Gril 显示「当前条件下无候选词」。

### 2.2 第一层：表层数据诊断

shop=7 OZON-Pt.Gril (tenant=4)：

| 表 | 行数 | 备注 |
|---|---|---|
| `platform_listings` | 372 ✓ | 商品同步正常 |
| `ad_campaigns` | 48 但**全 archived(42)+paused(6)，0 active** | |
| `ad_stats` | 262 行（最早 3-25） | 历史有数据，但 30 天内 0 行 |
| `product_search_queries` | 0 行 | Premium API 没拉到 |
| `seo_keyword_candidates` | **0 行 → rollup 显示空** | |

对比 OZON-Shario (shop=2)：候选池 3745 行 + ad_stats 382 行 + 318 orders ✓

### 2.3 第二层：用错指标判错根因

第一版分诊代码（`3e4d172`）用 `ad_stats` 判源数据 → Pt.Gril 有 262 行 → 返 `engine_not_run` → 让用户去手动跑引擎。

我直接生产试跑 `analyze_paid_to_organic(shop=7, days=30/60)` 全部 `analyzed_pairs=0`。

### 2.4 第三层：真根因 — Trafareti CPC 自动广告无关键词

读引擎 `self_sql` 发现 `JOIN ad_keywords kw` 是强依赖。查 `ad_keywords shop=7: 0 行` —— 这家店所有活动是 **Ozon Trafareti CPC（自动广告）**，没有关键词级数据，老张昨日 memory `project_ozon_keyword_stats_pending.md` 已有铺垫。

候选池引擎 `analyze_paid_to_organic.self_sql` 的真实链路：
```
ad_keywords → ad_groups → ad_campaigns → platform_listings → products
                      ↑ LEFT JOIN ad_stats（按 keyword_id 关联）
```

`ad_keywords=0` → 第一个 JOIN 就 0 行 → HAVING 也无数据 → 永远空。

**Pt.Gril SEO 优化建议页永远 not_ready 是业务正确**（不是 bug），新文案让用户知道是"广告类型选择问题 + Premium 订阅未同步"，不是系统坏了。

---

## 三、上午 commit 详细

### 3.1 `3e4d172` SEO 候选池空态 hint 分诊 v1（被自己的 v2 覆盖）

`compute_candidates_rollup` 在 `pool_count==0` 分支多两个 COUNT 判 ad_stats + product_search_queries：
- 都空 → `_CANDIDATES_NO_SOURCE_HINT`
- 有源 → `_CANDIDATES_ENGINE_NOT_RUN_HINT`

response.data 加 `not_ready_reason` 字段（`no_source` / `engine_not_run`），前端可选用，hint 字符串契约保持兼容。

### 3.2 `8c336ac` `generate_daily_report` 时区 aware/naive 减法 bug

老林昨日 §17/§18 全仓扫尾时漏了 `app/tasks/report_tasks.py`。task_logs 04-22/23 每天 5:00/5:05/5:10 三轮全失败 `can't subtract offset-naive and offset-aware datetimes`，**已挂 4 天**。

根因：line 50 `started_at=datetime.now(timezone.utc)`(aware) → `db.commit() + db.refresh()` 后从 naive DateTime 列重读变 naive；line 101 `finished_at(aware) - started_at(naive)` 抛 TypeError。

修法（规则 6 对齐）：4 处 `datetime.now(timezone.utc)` → `utc_now_naive()`，naive-naive 等价 timedelta，且与 DB DateTime(无 tz) 列直接匹配。import 清理掉 `datetime/timezone`。

smoke：同步触发 `generate_daily_report.apply()` → SUCCESS ✓ task_log id=1340。

### 3.3 `b9750ef` 候选池分诊 v2 — 改用 ad_keywords + Trafareti CPC 文案

二次诊断后修正 v1 的判错：
- 分诊查询从 `ad_stats` 改为 `ad_keywords`（精准对应引擎依赖）
- `_CANDIDATES_NO_SOURCE_HINT` 增第①档"自动广告类型"场景说明，Ozon 文案点名 Trafareti CPC
- `_CANDIDATES_ENGINE_NOT_RUN_HINT` 文案校准为"广告关键词或搜索词数据"

smoke：
- shop=7 Pt.Gril → `no_source` ✓ 真根因
- shop=2 Shario → ready, 200 items ✓
- shop=6 WB-Pt.Gril → `engine_not_run` ✓（有 ad_keywords 但池子没生成）

### 3.4 `6b1ee1d` `generate_daily_report` duration_ms 负数

8c336ac 修完后 smoke 出现 `duration_ms=-103`。根因：MySQL DATETIME(0) round 到秒（57.799 → 58），`db.refresh` 后 `task_log.started_at` 损失毫秒，`finished(57.902) - 58 = -98ms`。

修法：用 Python 局部 `started_at_local` 算 duration，不依赖 DB round 后的值；兜底 `max(0, ...)` 防其他罕见情况。

smoke：duration_ms=42 ✓ task_log id=1341。

### 3.5 `22d1ebb` `fetch_product_search_texts` 让 WBSellerQuotaExhausted 穿透

实测 04:00 MSK beat（实跑 01:53-55 UTC）：shop=5/6 cooldown 命中后还跑 3 批白等 40s。根因：`fetch_product_search_texts` line 1597 `except Exception` 把 `WBSellerQuotaExhausted` 也吞了（log warning + return []），上层 `refresh_shop` 的 `except WBSellerQuotaExhausted: break` 永远进不去。

修法：try 链最前面加 `except WBSellerQuotaExhausted: raise` 让异常穿透。

smoke：直接调 `refresh_shop(shop=5, days=7)` → `elapsed=0.02s` 第一批就 break ✓ errors 里规范记 `type=quota at_batch=1`（之前 40s+）。

---

## 四、04:00 MSK WB beat 首跑实情（推翻昨日预期）

老张昨日 §13.3 P0 #1 期望"明早 04:00 MSK 看是 quota reset 还是仍 cooldown"，老林昨日 P0 #2 期望"WB quota daily reset"。

**实测**（celery_worker.log 01:53-55 UTC）：
- **shop=1 WB-Shario**：`未开通 Jam，跳过`
- **shop=5 wb测试**：第一批 trip → batch 2/3 cooldown skip（修 22d1ebb 前的旧行为）→ `synced_queries=0`
- **shop=6 WB-Pt.Gril**：拿到 86 条 4-22 数据入库（部分批次成功后 trip）→ `synced_queries=86`
- 三店 quota 当前**仍熔断**（TTL ~40min，是 beat 自己 trip 的新熔断）

**结论**：WB 不是 daily reset，是滚动 24h 配额或更长。每次烧光要等 1h+ 冷却。需要更长周期观察才能下定论。

---

## 五、给老林沟通消息（用户转发）

5 条主题：
1. **真 bug 警示**：你 ca5a96b 熔断挪到 _request 后，fetch_xxx 内部还有 except Exception 吞 quota 的遗漏。请扫 wb.py 其他 fetch_xxx
2. **WB quota 不是 daily reset**：你昨晚 P0 #2 期望打脸
3. **shop=6 auto_execute 暂不能恢复**：三个绿灯 0/3
4. **协作注意**：你 ab895eb 的 quota_circuit_key_for_shop(shop_id) 我 search_insights/service.py:355 还在用旧 key
5. **5 commit 简列**

老林尚未回复（用户刚转发）。

---

## 六、遗留

### 强阻塞等用户
- Yandex 正确 Token（长期）
- WB 后台"顶级搜索集群" CSV 样本（多日 P0）
- WB-Shario (shop=1) 是否开 Jam 订阅（今天发现没开）

### 等环境恢复
- WB seller quota 全员 cooldown（TTL ~40min）— 等下午观察一轮 beat
- 候选池引擎 `analyze_paid_to_organic` 给 WB shop=6 跑（quota 恢复后）

### 等老林回复
- ab895eb seller_id 隔离 key 怎么对接（service.py:355 旧调用）
- wb.py 其他 fetch_xxx 是否也有 quota 异常吞掉模式
- shop=6 auto_execute 何时恢复

### 代码层（非阻塞）
- `CandidatesRollupTable` 加"刷新引擎"按钮入口（昨日遗留）
- 是否加 `seo-engine-daily` beat MSK 05:00 自动触发 `analyze_paid_to_organic`（昨日遗留）
- SSE keepalive `asyncio.to_thread` 重构（多日观察期）

---

## 七、今日体会

1. **"用错指标会让根因诊断转两圈"**
   v1 用 `ad_stats` 判源数据，结果 Pt.Gril 有 262 行历史 ad_stats 但 0 行 ad_keywords，
   分诊给出 `engine_not_run`，让用户去跑引擎 → 全空 → 再回来。**真正应该判的是引擎 SQL 强依赖的表**（这里是 ad_keywords），不是表面"广告统计"。
   下次写分诊判断前必须先读一遍引擎 SQL 的 JOIN 链，确认从哪个表"上游"开始决定结果。

2. **"全仓扫尾要扫所有目录，不是 service 优先"**
   老林昨日 §18 说"规则 2/6 全仓 0 残留"，但实际 `app/tasks/report_tasks.py` 漏了 —
   每天 daily report 三轮全挂已 4 天没人发现。**`grep -rn "datetime.utcnow\|datetime.now()" app/`
   要扫到 app/ 根，不能只扫 services/ + api/**。今天我修这个时也只查了 `report_tasks.py`，
   `app/tasks/` 其他文件可能还有同问题（待老林扫）。

3. **"`except Exception` 吞自定义异常是埋雷"**
   `fetch_product_search_texts` 内部 `except Exception` 兼容性写法，把老林新加的
   `WBSellerQuotaExhausted` 也吞了。这是经典"自定义异常被通用 catch 拦截"的反模式。
   **新加 raise xxxError 时要检查全调用链有没有 except Exception 把它吞了**，否则
   raise 不到上层等于没加。

4. **"日志里看到 cooldown 还能继续 sleep 是强信号"**
   shop=5 日志显示 batch 1/2/3 三连 quota cooldown，每批之间还有 20s sleep — 这种
   "明知道挂了还在等" 的模式 99% 是异常没穿透。读日志时遇到"重复失败 + 固定间隔"
   要立刻怀疑异常吞掉而不是 retry 策略。

5. **"用户单店报错暴露多层 bug"**
   用户一句"OZON-pt-gril 没数据" 暴露了：①hint 文案不准确（已修 v1→v2）；②老林全仓扫尾
   漏 report_tasks.py（已修）；③老林熔断到 _request 但 fetch_xxx 仍吞异常（已修）；
   ④WB 配额不是 daily reset（认知刷新）；⑤Trafareti CPC 自动广告无关键词数据（业务认知）。
   **客户报障 1 句 = bug 5 条，每次都比 review agent 收获大**。

---

**老张** 2026-04-24 上午收工

5 commit + 1 段日志 + 1 段给老林的同步（用户转发）。
真 bug 1 条（fetch_xxx quota 异常吞掉），漏修补 1 条（report_tasks 时区），
SEO 候选池 hint 分诊从 v1 错到 v2 对（用户原始问题答完整）。
等老林回复 + WB quota 自然恢复 + 用户给 CSV 样本。

---

## 八、上午 11:00 — 老林回复 + 验证 8c2f22c

老林对 5 条同步全部回复（用户转发），核心两件事：

### 8.1 老林 8c2f22c — 一行根治 fetch_xxx 吞 quota（替代 N×raise）

我提示"扫 17 个 fetch_xxx 是否同样吞异常"，老林 Explore agent 一查：的确 17 个端点
都是 `except Exception: return []` 模式（fetch_keyword_stats / fetch_min_bid /
fetch_excluded_keywords / fetch_campaign_keywords / fetch_campaign_products /
fetch_campaign_summary / fetch_prices / fetch_daily_stats / _patch_bids_once /
fetch_sales_returns_by_region / fetch_region_sales_by_sku / fetch_region_sales /
fetch_commissions / 加我 22d1ebb 显式补的 fetch_product_search_texts）。

**没在 14 处分别补 `except WBSellerQuotaExhausted: raise`**，改成
**`WBSellerQuotaExhausted(BaseException)`**（asyncio.CancelledError 同思路，
Python 3.8+ 官方就是这么改的）。`except Exception` 自动放行，显式
`except WBSellerQuotaExhausted as e:` 不受影响。

我 22d1ebb 的显式 raise 老林明确说"保留作防御文档化，不冲突"。

**实证 MRO**：`[WBSellerQuotaExhausted, BaseException, object]`,
`isinstance(Exception)=False, isinstance(BaseException)=True` ✓

### 8.2 老林同 commit 顺手改了 search_insights/service.py:355 pre-check

我同步消息 #4 提到的"旧 shop_key 调用还在"，老林同 commit 8c2f22c 改成
**seller_ 优先 + shop_ fallback**：
```python
seller_id = (getattr(shop, "platform_seller_id", None) or "").strip() or None
if seller_id:
    ttl_seller = r.ttl(quota_circuit_key_for_seller(seller_id))
    ...
if not cooldown:
    ttl_shop = r.ttl(quota_circuit_key_for_shop(shop.id))
```
`getattr` 兜底以防某些调用路径传的是 Row 不是 ORM 对象。

### 8.3 ab895eb 止血落地实证

shops 表 `platform_seller_id` 持久化结果（直接查 DB）：
| shop | name | status | platform_seller_id |
|---|---|---|---|
| 1 | WB-Shario | active | `ad6e754f-b655-4c71-8857-33dcc396e1e5` |
| 5 | wb测试 | **inactive**（用户方案 A 暂停） | `ad6e754f-b655-4c71-8857-33dcc396e1e5` |
| 6 | WB-Pt.Gril | active | `f916ae65-a3dc-48f5-beca-fb737b0fe92b` |

shop=1/5 同 seller A，shop=6 独占 seller B。

Redis cooldown key 现状：
- 旧 `wb:seller_quota_exhausted:shop_*` 全 ttl=-2（不存在）✓ 转换脚本干净
- 新 `wb:seller_quota_exhausted:seller_ad6e754f` ttl=3067s（shop 1+5 共享）
- 新 `wb:seller_quota_exhausted:seller_f916ae65` ttl=-2（老林上午手动删了让 shop=6 用户能用）

### 8.4 我自找的事故 — probe 把 shop=6 的 cooldown 又 trip 回去

为了实测 BaseException 在真实流量下表现，单 nm probe `fetch_product_search_texts(nm=260342372, date=2026-04-22)`：

```
WB seller quota circuit TRIPPED seller=f916ae65 (triggered by shop_id=6) TTL=3600s
shop=6 quota exhausted: ... per seller f916ae65; See https://dev.wildberries.ru/...
```

**结论**：
- ✓ BaseException 在真实抛出/catch 链路下完美验证（异常正常上抛 + `except WBSellerQuotaExhausted as e:` 抓到）
- ✗ shop=6 quota 实际还没恢复 — 老林上午用 `/promotion/count` (read 端点) probe 200 不能代表 search-texts 端点 quota 也够。**同一 seller 不同 endpoint-group 配额独立**
- ✗ 我把"shop=6 三绿灯第 1 条 quota_status items 空"撤回了，向老林致歉

### 8.5 收获 → memory

把这次 probe 经验追加进 quota memory（老林已写过 04-24 实测段，再加一条）：
- **probe 是否恢复必须用同 endpoint**，不能用别的端点判定
- search-texts 端点 quota 比 promotion/count 更紧

让老林选择是他追加还是我追加（避免双写）。

### 8.6 老林承认的漏扫死角

老林明确承认 04-23 §17/§18 全仓扫尾时漏了 `app/tasks/report_tasks.py` —
他的自查命令 `grep date.today()` 只扫到规则 6 残留，没扫到 `datetime.utcnow()`
（属于规则 2）。**正交规则的 grep 命令不能合并，每条规则一个独立 grep**。
老林已加进他的 followup memory。

### 8.7 等三件事

1. ✅ shop=5 inactive，beat 跳过它，节省 seller A 一份配额
2. 🟡 shop=6 自然冷却 1h（我自己 trip 的，~02:28 UTC + 1h ≈ 03:28 UTC 自动消失）
3. 🟡 shop=1 自然冷却 ~50min（继承上午 04:00 MSK beat trip）
4. 🟡 **今晚或明早 04:00 MSK beat 第二次跑结果** — 验证 BaseException + pre-check 真实流量表现：
   - cooldown 未过期：pre-check 0ms skip，不进循环 ✓
   - cooldown 过期：第一批撞 quota → BaseException 穿透 → 上层 break ≤ 1 batch（不再 3 批白等 40s）

### 8.8 commit 追加

```
（无我自己的代码 commit，只有老林的 8c2f22c 拉过来 + 当前段日志）
959d815 docs(daily): 老张 04-24 上午 — SEO 候选池 hint 分诊 v2 + report_tasks 时区扫尾 + fetch_xxx quota 异常穿透
（本）   docs(daily): 老张 04-24 §八 — 老林回复 8c2f22c 验证 + probe 自找的小事故
```

---

## 九、再次体会

1. **"提一个 bug 引出更大的根治方案"**
   我 22d1ebb 修了 `fetch_product_search_texts` 1 处穿透；老林一行
   `BaseException` 替代我潜在要写的 17 处 `except WBSellerQuotaExhausted: raise`。
   **同一问题不同抽象层级的解法效率差 17 倍**。但前提是我先报告全 17 处的范围，
   老林才有信号"这是规模性问题"。**"我修了一处，但还有 17 处"比"我修了一处"
   信息量大十倍**，下次提 review 都要带上类似的"全集范围"信号。

2. **"探针不能跨 endpoint 判定"**
   老林上午 probe `/promotion/count` 200 → 删 cooldown → 用户能用。我下午用
   `/search-texts` 又 trip 一次。同一 seller 不同 endpoint-group 池子独立这件事
   两个人各踩一次。**memory 里要写清楚"探针的 endpoint 必须和实际业务的
   endpoint 匹配，不能借另一端的成功推断"**。这种"一种成功不能推一切成功"的
   反模式昨日 §十一.1 已栽过一次（老张写的），今天再栽 — **共同的反模式比孤立 bug
   更值得警惕**。

3. **"BaseException 用于流控信号"是工业界经典**
   老林讲 asyncio.CancelledError 同思路，Python 3.8+ 官方就把它从 Exception
   挪到 BaseException 子类。**自定义异常的"作为业务错误"还是"作为流控信号"
   决定了它该继承谁**。流控信号用 BaseException 就是为了避开下游 catch all 网。
   下次设计 raise xxxError 时先问："上游业务正常应该 catch 我吗？" — 答案"不应该"
   就上 BaseException。

---

**老张** 2026-04-24 真上午收工

5 commit + 1 段长日志（八/九/总览） + 给老林两轮同步（一发一回）。
真 bug 1 条 + 顺手扫尾 1 条 + Pt.Gril 用户问题答完整 + 老林根治方案验证 ✓ + 1 次自找的 probe 事故。
等今晚 / 明早 04:00 MSK beat 第二次跑结果验证 BaseException 真实表现。

---

## 十、中午 — 用户二次报 Pt.Gril 两家都没数据 + 手动救活 WB 一家

### 10.1 用户反馈

> "SEO管理 优化建议 ozon-pt.gril 和 wb-pt.gril 都没有数据"

上午修的 hint 文案 v2 已上线，但用户关心的是"我要真数据"不是"你告诉我为啥没数据"。

### 10.2 深入诊断 — v2 分诊判断逻辑对，但之前推断错

**shop=7 Ozon-Pt.Gril**：v2 返 `no_source`（Trafareti CPC 无关键词 + 无 Premium 同步），**真救不了**。

**shop=6 WB-Pt.Gril**：v2 返 `engine_not_run`。我一开始以为也是 `ad_keywords=0` 所以没源数据 —— 但实际：
- `ad_keywords`: 0 行（WB 自动广告类型无关键词，和 Pt.Gril Ozon 端一样）
- `ad_stats 30d`: 2628 行 orders=3846 spend=₽238k（广告真在跑）
- **`product_search_queries`: 85 行**（老张今晨 04:00 MSK beat 拉到的 4-22 数据）
- `seo_keyword_candidates`: 0 行（引擎从未跑过这家店）

关键：引擎 `analyze_paid_to_organic` 有 **Step E** 专门吃 `product_search_queries`（源 B organic self，门槛 `frequency≥5 OR orders≥1`）。**psq 有 85 行 → 引擎能产出候选**，只是没人跑过。

### 10.3 直接跑引擎救活

```
analyze_paid_to_organic(shop=6, days=30) →
  analyzed_pairs=85 / candidates=85 / written=85
```

候选池从 0 → 85 行。rollup 再查：`data_status=None items=72 total=72`（72 是合并去重后的 unique keyword 数）。

真实关键词样例：`серьги булавки` / `серьги серебро` / `серьги золотые 585 женские`（饰品词，与店铺品类契合）。**用户刷新 SEO 优化建议页 → 选 WB-Pt.Gril 应该就看到 72 条候选**。

### 10.4 暴露的缺口 → 记录到 memory

候选池引擎**没有定时任务**。WB/Ozon sync → psq 有新数据 → **候选池不会自动更新**。用户每次看到"没数据"要人工帮忙跑引擎。已踩两次（04-23 晚 + 04-24 中午）。

用户拍 **"记录 下次沟通"**，未决策，落 memory：
- `memory/project_seo_engine_daily_beat_pending.md`（3 个备选方案 A/B/C）
- `MEMORY.md` 加一行指向该 memory

### 10.5 commit 追加（本段）

```
（本） docs(daily): 老张 04-24 §十 — 用户二次报 + 手动救活 shop=6 候选池 + engine beat 待议
```

---

## 十一、今日完成的接口清单

| 方法 | 路径 / 函数 | 动作 | commit |
|---|---|---|---|
| GET | `/seo/shop/{id}/candidates-rollup` | response.data 新增 `not_ready_reason` 字段（`no_source` / `engine_not_run`），hint 文案重写分诊 | `3e4d172` → `b9750ef` |
| — | `_CANDIDATES_NO_SOURCE_HINT` / `_CANDIDATES_ENGINE_NOT_RUN_HINT` | **新**：替代旧 `_CANDIDATES_NOT_READY_HINT`，Ozon 文案点名 Trafareti CPC 自动广告场景 | `b9750ef` |
| — | `compute_candidates_rollup` 空态分诊 | **改**：判断条件从 `ad_stats+psq` 改为 `ad_keywords+psq`（精准对应引擎 self_sql 依赖） | `b9750ef` |
| — | `generate_daily_report` Celery 任务 | **改**：4 处 `datetime.now(timezone.utc)` → `utc_now_naive()`；规则 6 合规。修连续失败 4 天的 `TypeError: can't subtract offset-naive and offset-aware datetimes` | `8c336ac` |
| — | `generate_daily_report` duration_ms | **改**：用 Python 局部变量 `started_at_local` 算 duration，不依赖 DB refresh 后的 round 秒精度；兜底 `max(0, ...)` | `6b1ee1d` |
| — | `WBClient.fetch_product_search_texts` | **改**：try 链加 `except WBSellerQuotaExhausted: raise`，让熔断异常穿透 `except Exception` 兜底（refresh_shop 顶层 break 才能生效） | `22d1ebb` |
| — | `analyze_paid_to_organic(shop_id=6)` | **手动触发一次**：候选池从 0 → 85 行（不是代码改动，是给用户救火） | — |

老林同主线并行的 commit（协作关联）：
- `ab895eb` WB quota 熔断 key 改按 seller_id 隔离（shops 多 shop 共享冷却）
- `8c2f22c` `WBSellerQuotaExhausted(BaseException)` 根治 + search_insights pre-check 走 seller key（替代我在 14+ 个 fetch_xxx 各自补 raise 的工作量）

---

## 十二、遗留

### 🔴 强阻塞等用户
- Yandex 正确 Token（长期多日 P0）
- **WB 后台"顶级搜索集群" CSV 样本**（多日 P0，老林昨日再追）
- **WB-Shario (shop=1) 是否开 Jam 订阅**（今天发现没开，用户业务决策）
- **Ozon Premium 订阅开通**（如果想让 Pt.Gril 有候选，唯一路径）

### 🟡 等环境恢复
- **WB seller A cooldown（shop=1）**自然消失 ~50min
- **WB seller B cooldown（shop=6）**自然消失 ~58min（我下午 probe 自找 trip 的）
- **今晚 / 明早 04:00 MSK beat 第二次跑**：验证老林 8c2f22c BaseException + pre-check 在真实流量下表现（cooldown 命中 0ms skip / cooldown 过期则第一批 trip → break ≤ 1 batch 不再 sleep 40s）

### 🟢 代码层技术债（非阻塞）
- **`seo-engine-daily` beat 待议**（用户拍"下次沟通"，方案 A/B/C 见 memory `project_seo_engine_daily_beat_pending.md`）
- `CandidatesRollupTable` 加"刷新引擎"按钮入口（昨日 §13.3 P1 #5 同一主题）
- `app/tasks/` 其他文件是否还有 `datetime.utcnow()` 漏扫（老林 followup，不是我主导）
- SSE keepalive `asyncio.to_thread` 重构（多日观察期）
- 亲和度 0.5 / 占比 0.03 阈值一周数据验证（多日）

### 🔵 已知但业务正确
- **Ozon-Pt.Gril (shop=7) 候选池永远 not_ready**：Trafareti CPC 无关键词级数据，业务现象，不是 bug。要救只能开 Premium 订阅让 psq 有数据（和 WB-Pt.Gril 同路径）

---

## 十三、明天（2026-04-25）优先级

### P0
1. **看昨晚 / 今早 04:00 MSK WB beat 第二次跑结果**：
   - 验证老林 8c2f22c BaseException 真实穿透表现
   - 看 `wb-search-texts-daily` 是否拉到新 product_search_queries（quota 恢复的真实信号）
   - **关键**：如果 shop=6 psq 有新数据进，手动再跑一次 `analyze_paid_to_organic` 让候选池继续更新（或者这时候可以正式讨论 seo-engine-daily 方案）

2. **追用户要 WB 后台"顶级搜索集群" CSV 样本**（多日未决，054 迁移 oracle 方案阻塞中）

3. **跟老林讨论**：
   - `seo-engine-daily` beat 是否做（方案 A/B/C，用户已同意记录待议）
   - shop=6 `auto_execute=1` 是否可以恢复（三绿灯状态取决于昨晚 beat 结果）
   - `app/tasks/` 全仓 `datetime.utcnow` / `datetime.now()` 的 follow-up grep（老林承认 04-23 扫尾漏了这一批）

### P1
4. 如果 shop=6 auto_execute 恢复 → 观察 AI 调价第一次自动执行后的 bidlog（老林昨日建议）
5. 如果用户拍做 `seo-engine-daily` beat：30 min 工作量（seo_engine_task.py + celery_app beat 条目 + smoke + 部署）
6. `CandidatesRollupTable` 加"刷新引擎"按钮（闭环 rollup 模式空态，10min）

### P2
7. 前端 `dayjs.utc` cutover 前老数据展示错 8h（老林 §16.6 遗留）
8. 亲和度阈值一周数据验证
9. 观察 supervisor 治本累计 uptime（昨日累计无 spawn error，持续观察）

---

**老张** 2026-04-24 完整收工

5 代码 commit + 3 段日志（上午 / §八九 / §十-§十三）+ 2 段给老林同步 + 2 条新 memory（Trafareti CPC / SEO engine beat 待议）+ 1 次给用户救火（WB-Pt.Gril 候选池 85 行）。

今日主题：**用户报 "Pt.Gril 没数据" 引出 4 层修复**（hint 文案分诊 / 时区扫尾 / quota 穿透 / 候选池手动救活）+ **老林根治方案验证**（BaseException 替代 14 处 raise）+ **暴露 SEO 引擎无定时任务缺口**（待议）。

明早看 beat 结果 + 等老林回复 + 等用户给 CSV / 业务决策。
