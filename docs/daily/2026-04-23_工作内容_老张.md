# 2026-04-23 工作内容 — 老张（后端）

> 今日主线：**昨日 P1 清单扫尾 — AI 调价纵深防御 / 冗余索引清理 / WB 聚类脏词防护**
> 上午 4 个 commit + 1 个迁移，全是昨日遗留的 P1 小步动作，没新开大坑。

---

## 一、启动恢复

按 CLAUDE.md 流程读了：
- `docs/daily/2026-04-22_工作内容_老张.md`（自己昨日 34 commit + 5 段日志 — WB 5 簇对齐 / SEO 店级 Tab / supervisor 治本 / 类目扩散 SUM bug 修）
- `docs/daily/2026-04-22_工作内容_老林.md`（老林 24 commit — AI 调价清洗规则 v3 / 莫斯科自然日切换 / Tooltip 动态化 / 6 个生产事故）
- `docs/api/bid_management.md`

---

## 二、P0 自然日评估检查（5 分钟）

老林昨晚 P0 "看凌晨 0:05 MSK 第一次自然日评估"。实际诊断：

**自动触发未跑** — 5 家店全部 `ai_pricing_configs.is_active=0`，celery beat `bid-management-hourly` 遍历时都 skip。昨晚的 `bid-management-hourly` 在 `2026-04-23 05:05:00`（= 0:05 MSK）正常触发，但 `_process_shop` 判 `is_active=0` 直接 return。

**但手动触发下功能正常**：
- 用户今早 09:54 北京（= 04:54 MSK）点了立即分析，shop=6 `last_executed_at=2026-04-23 09:54:35`
- 生成 22 条 pending 建议 / 26 个活动
- 27 个 hill_base：**17 涨 / 10 跌 / 0 不动**（相比老林昨日 "12 涨/15 跌" 略向涨偏移，分布仍合理）
- `hill_last_eval_at`：04-23 共 26 个 / 04-22 仅 1 个 → `74335b8`（莫斯科自然日切换）+ `7908bf8`（一天一评护栏）**双生效** ✓
- 22 条全部 `suggested < current` 是表象 — reason 里 17 个 "方向+1 base 涨" vs 10 个 "方向-1 base 跌"，本质是 **4:55 MSK 凌晨低谷系数 0.70** 压的（`suggested = base × 0.70` 就显示为降价）

**supervisor 治本首夜验证**：三服务 uptime 11h37m 无重启 ✓ 零 fastapi spawn error。

**🔔 关键发现（非我 commit）**：今早 10:43 另一并行会话推了 `0a30a5b fix(bid): 旧 AI 调价 UI 开关改走 enable/disable 接口写入 is_active` — 原来 `AdsAIPricing.jsx` 的开关只写 `auto_execute` 不写 `is_active`，**所有店铺 is_active=0 就是这个 bug 导致**。修复后用户只要在新 UI 打开 shop=6 的开关，下次 0:05 MSK 就会真评估。

---

## 三、P1-A：`_calc_healthy_window_*` 加 platform 纵深过滤 `c729ff8`

### 3.1 背景

老林昨日 general-purpose agent 审查出的中危：两函数的 SQL 只按 `(tenant_id, campaign_id, ad_group_id)` 过滤，没带 platform。

### 3.2 生产验证

```sql
SELECT tenant_id, campaign_id, ad_group_id, COUNT(DISTINCT platform) pn
FROM ad_stats GROUP BY tenant_id, campaign_id, ad_group_id HAVING pn > 1
-- 结果：0 行
```

当前 `(tid,cid,agid)` 100% 隐含唯一 platform（因为 `ad_campaigns.id` 每行绑一个 platform，不跨平台共享 id）。**业务上不加 platform 也不会错**，但若 `ad_stats` 写入端出 bug 混入跨平台脏行，清洗结果会被污染 — 这是 agent 判中危的出发点。

### 3.3 改动

- `_calc_healthy_window_metrics` / `_calc_healthy_window_full` 新增 `platform: str | None = None` 参数
- SQL 动态拼 `AND platform=:platform`（只有传了 platform 才加）
- `_hill_climbing_decision` 顺道加 `platform` 参数，转发给内部 3 处调用
- `analyze_now` 两处调用点（line 1322 亏损建议、line 1400 hill 决策）传 `platform=platform`（循环中 `platform = shop.platform`）

默认 None 保向后兼容；调用方不传则退化为原行为（任何单测不需要改）。

### 3.4 合规自查

- 规则 1：SQL 里 `tenant_id` 原本就有 ✓
- 规则 4：不涉及手动触发接口
- 纵深防御思路同规则 1（每条 SQL 必带 tenant_id）

---

## 四、P1-B：清理 052 冗余索引 `e6a0fb1` + 迁移 053

### 4.1 EXPLAIN 生产验证

```sql
EXPLAIN SELECT s.id, ag.user_managed
FROM ai_pricing_suggestions s
LEFT JOIN ad_groups ag ON ag.tenant_id=s.tenant_id AND ag.campaign_id=s.campaign_id
                       AND ag.platform_group_id=s.platform_sku_id
WHERE s.shop_id=6 AND s.status='pending';
```

默认：`key=uk_campaign_sku, type=eq_ref, rows=1`（优化器选 UNIQUE 索引）
IGNORE idx_camp_pgid（模拟 DROP）：**完全一样** `key=uk_campaign_sku`

结论：`idx_camp_pgid` 从未被使用，DROP 零风险。

### 4.2 迁移 053

文件：`database/migrations/versions/053_drop_ad_groups_idx_camp_pgid_redundant.sql`
内容：`ALTER TABLE ad_groups DROP INDEX idx_camp_pgid`

执行：`bash deploy_remote.sh --migrate 053_drop_ad_groups_idx_camp_pgid_redundant.sql` → 成功
验证：DROP 后重跑 EXPLAIN，仍 `key=uk_campaign_sku type=eq_ref rows=1` ✓

### 4.3 踩坑

首次调用 `bash deploy_remote.sh --migrate database/migrations/versions/053_xxx.sql` 报 "No such file or directory"。脚本 L83 已经硬编码 `mysql ... < database/migrations/versions/$1`，传完整路径导致拼接出错 — **只能传文件名**。脚本 L10 注释写了，我没看。

---

## 五、P1-C：WB 集群代表词黑名单 `6e72f25`

### 5.1 背景

昨日 B1：DeepSeek AI propose 阶段偶发非 WB 真实簇的代表词（`сережки с котиками золото` / `для девочки в подарок`）。这些词被 WB set-minus 测试"接受"（WB 接受 ≠ 真是集群代表），按原逻辑会持久化到 known_reps 污染下次查询。

### 5.2 设计

加 Redis 黑名单集合，`add_known_rep` 写入前检查，黑名单词直接 log + skip。

- Key: `wb:cluster_rep_blacklist:{advert_id}:{nm_id}`
- TTL：90 天（长于 known_reps 30 天，防过期后脏词复活）

### 5.3 改动

`app/services/ad/keyword_clustering.py`：
- 新增 `is_rep_blacklisted(adv, nm, rep) → bool`
- 新增 `add_rep_to_blacklist(adv, nm, rep) → int`：sadd 黑名单 + 同步从 known_reps 清除，返回清除数
- 新增 `remove_rep_from_blacklist(adv, nm, rep) → bool`：撤销误加
- 新增 `get_rep_blacklist(adv, nm) → List[str]`
- **改造 `add_known_rep`**：写入前检查黑名单，在则 `logger.info` 跳过

`app/api/v1/ads.py` 扩展 `POST /campaign-keywords/{id}/probe-cluster-rep`：
- Request 加 `action: str` 字段，默认 `"probe"`
- `action=blacklist`：直接拉黑 + 清 known_reps（不走 WB 验证）
- `action=unblacklist`：撤销
- `action=probe`：原行为
- 清 clusters 缓存抽成局部函数 `_bust_cluster_cache` 三 action 共用

### 5.4 生产 smoke test（6 场景全绿）

```
1. 初始: known_reps=[], blacklist=[]
2. add_known_rep(dirty) → known_reps=['сережки с котиками золото']
3. add_rep_to_blacklist(dirty) → removed=1, known_reps=[], blacklist=[dirty]
4. add_known_rep(dirty) 再次 → [log: 跳过黑名单] known_reps=[]
5. remove_rep_from_blacklist(dirty) → blacklist=[]
6. add_known_rep(dirty) 又能写 → known_reps=[dirty]
```

### 5.5 部署附带验证

`bash deploy_remote.sh --backend` stop→start 全顺，**fastapi 秒起不需要 fuser -k 8000/tcp**。supervisor 治本第二次实战验证。

---

## 六、P1-D：Review 老林昨日 24 commit

### 6.1 我手动扫的敏感点

| 点 | 结论 |
|---|---|
| `_save_hill_state` ON DUPLICATE KEY UPDATE | line 398 `tenant_id = :tid` ✓ 合规规则 1 |
| `_get_roas_21_30`（老林 6c21d33 修）| `tenant_id + platform + sku + campaign_id` 全过滤 ✓ |
| `analyze_stream` keepalive（11f8385）| `inner_task = _asyncio.create_task(...)` + `wait_for(shield, 10s)` + timeout 时 `yield ": keepalive\n\n"` — **异步设计正确**，但若 `_analyze_now_inner` 里有同步 `db.execute` 调用会阻塞事件循环 → keepalive 也发不出。老林已标 P1 遗留（用 `asyncio.to_thread` 包 DB 操作）|
| `_calc_healthy_window_*` 的 platform | 今日我补齐（§三） |

### 6.2 Explore agent 补漏（**交给老林**，我不动文件）

用户拍"老林在改"→ 我不继续动 `ai_pricing_executor.py` / `bid_management.py` 避免 commit 冲突。下面清单**交给老林**按需处理：

| # | 位置 | 发现 | 严重度 |
|---|---|---|---|
| R1 | `ai_pricing_executor.py:878` | `update_config` 初始 SELECT `WHERE shop_id=:shop_id LIMIT 1` 缺 `AND tenant_id=:tenant_id`。虽然后续有 tenant_id 检查，但首查就是"枚举式漏洞"的典型入口 | **中** 规则 1 |
| R2 | `ai_pricing_executor.py:1006` | `_schedule_auto_execute` `WHERE shop_id=:shop_id AND is_active=1 LIMIT 1` 缺 tenant_id — 任意 shop_id 可触发该租户的 scheduled 逻辑 | **高** 规则 1，跨租户触发 |
| R3 | `ai_pricing_executor.py:2385` | `_execute_hill_decision` 的 `INSERT ... ON DUPLICATE KEY UPDATE` 漏 `SET tenant_id = :tid` — 跨租户 UNIQUE 冲突时会污染其他租户的 tenant_id 指针 | **中** 规则 1 |
| R4 | `bid_management.py:638` | `approve_suggestion` 无 `shop_id` query 参数，仅内部 service 按 tenant_id 检查。规则 4 要求所有手动触发接口必须按 shop_id 前置过滤 | **低** 规则 4（service 层已纵深） |
| R5 | `bid_management.py:730` | `remove_product_from_campaign` 同上，shop 验证在 handler 内部而非签名前置 | **低** 规则 4 |
| R6 | `ai_pricing_executor.py:1070` keepalive | 昨日老林自己标遗留，无新增结论 | **P1** 已记 |
| R7 | `ai_pricing_executor.py:395` `UTC_TIMESTAMP()` | 依赖 MySQL session 时区。生产 MySQL 若未固定 UTC 会拿本地时间 | **低** 当前生产是 UTC，无即时风险 |

**我的建议给老林**：
- R2 最优先（跨租户触发漏洞，不需要很大改动：加 `AND tenant_id = :tenant_id` 一行）
- R1 / R3 顺手补（同一文件 + 相同改动模式）
- R4 / R5 规则 4 路由层补 `Depends(get_owned_shop)` 或加 `shop_id` query
- R6 老林原计划就要做
- R7 无即时风险，可列 P2

---

## 七、今日 commit

```
c729ff8 fix(ai-pricing): _calc_healthy_window_* 加 platform 纵深过滤
e6a0fb1 chore(db): 053 迁移 DROP ad_groups.idx_camp_pgid 冗余索引
6e72f25 feat(ads): WB 集群代表词加脏词黑名单防 DeepSeek 污染 known_reps
(本) docs(daily): 老张 04-23 — P1 清单 4 件扫尾
```

并行（非我）：
- `0a30a5b fix(bid): 旧 AI 调价 UI 开关改走 enable/disable 接口写入 is_active` — 今早 10:43 有人推，解决"is_active 一直 0"的根因

---

## 八、给老林同步

1. **是否建议用户开 shop=6 `is_active=1 + auto_execute=0`？** 现状：is_active=0 → 爬山法靠用户每天手动点一次才推进 base。开 is_active 后每晚 0:05 MSK 自然日切换自动评估，但不自动执行建议（用户仍需人工审批）。**你拍**。
2. **`_calc_healthy_window_*` platform 纵深防御已补**：`c729ff8`。默认参数 None 向后兼容，只有 `_hill_climbing_decision` 调用链会传 `shop.platform`。
3. **052 索引冗余已删**：`e6a0fb1` + 迁移 053 已跑。生产 EXPLAIN 验证 DROP 前后都走 `uk_campaign_sku`，零性能影响。
4. **SSE keepalive 重构**：我看了 `_analyze_now_inner` 结构，异步设计正确但内部 DB 操作是同步的。建议优先级你定（P1 但不紧）。

---

## 九、遗留

### 强阻塞等用户
- Yandex 正确 Token（长期）
- WB Jam 订阅（SEO Tracking + 可能解锁 cluster 但昨日 44+ 端点 0 命中证明解锁不了）
- **WB 后台 CSV 样本**（老张昨日 P0，用户答应给）

### 今日推后 / 观察
- 亲和度 0.5 / 占比 0.03 阈值一周数据验证（昨日 §二）
- SEO service 层组合路径测试补充（多日遗留）
- 店级 TOP Tab 也加多选店铺（与按商品看一致）
- 类目扩散 `service.py` 根本性改造（影响 seo_keyword_candidates 现有数据，需迁移）
- `11f8385` SSE keepalive 用 `asyncio.to_thread` 重构（老林 P1）

### 今日新发现
- 并行 commit `0a30a5b` 揭示"AI 调价 UI 开关不写 is_active" bug — **这就是为什么昨晚 0:05 MSK 自动评估没触发**，修了。后续：`AdsAIPricing.jsx` 与 `BidManagement.jsx` 两套 UI 并存，长期要统一。

### 前端侧（我没动）
- 黑名单 UI 入口：`action=blacklist` 后端已通，前端 AdsOverview 可加"标记为脏词"按钮 — 等用户说要不要做
- 类目推断行显示优化（昨日 §三十三）

---

## 十、今日体会

1. **"推进 P1 清单比追新功能更显价值"**
   昨日 34 commit 是高峰，今日 4 commit 全是 P1 扫尾。每一个都是老林/我昨日标的技术债：platform 纵深、冗余索引、脏词防护。**P1 不清完就去做新功能，等于每天在地雷上堆东西**。连续两周小步修债务，系统越来越干净。

2. **"部署验证 = 附带收益"**
   `deploy_remote.sh --backend` 跑了 2 次，每次都验证一次 supervisor 治本（昨晚 `f8a42f9` 的 `stopasgroup=true + killasgroup=true` + `stopwaitsecs`）。**不用专门写 E2E，只要功能部署就免费 attest 一次治本效果**。这种"改动 + 部署"的组合拳对运维改动特别好用。

3. **"EXPLAIN 证明 DROP 索引安全"比争论更快**
   老林只说"052 索引冗余"，没给证据。我花 3 分钟写 EXPLAIN 脚本对比默认 vs `IGNORE idx_camp_pgid`，结果两者都走 `uk_campaign_sku`。**一个 EXPLAIN 能结束所有讨论**。生产 DDL 之前的 5 分钟 EXPLAIN 是最便宜的保险。

4. **"黑名单设计的 TTL 要长于白名单"**
   known_reps 30 天 / blacklist 90 天。**原因**：TTL 错配会导致"脏词在 known_reps 过期后又被 propose → 重新拉黑" 的振荡。blacklist 更长就是"记性更长"。这是 Redis 集合 TTL 设计的通用模式。

5. **"并行 commit 改变诊断结论"**
   我 10:30 诊断 "is_active=0 导致 celery 跳过"，10:43 有人推 `0a30a5b` 修了根因。**发 review 报告前必须 `git log` 一次**，否则你的结论可能已经被别人的 commit 推翻。日志里写清楚哪些是我做的、哪些是别人做的。

---

**老张** 2026-04-23 上午收工

4 commit + 1 迁移 + 1 段日志 + 1 次 Explore review（结果待补）。AI 调价纵深 / 索引清理 / 脏词黑名单三条 P1 全清。下午等用户验收 + WB CSV。

---

## 十一、下午：WB Jam 订阅开通 → 搜索词洞察实战打磨（~6 commit）

用户下午拍"WB Jam 订阅好了，用 shop=6 测试"。2026-04-18 骨架已落地等订阅，现在上线。

### 11.1 一次成功 + 一次灾难 → 实测字段 + 触发卖家级限流

首次单品裸调 shop=6 `nm_id=244456218` 一次就 200 拿回完整数据，核心字段假设全中：
- items 路径 `data.items` ✓
- `frequency/openCard/addToCart/orders/medianPosition/cartToOrder` 全是 `{"current": N, "percentile": P}` dict → `_cur()` parse 正确
- items[i] 带 **`nmId` 字段**（旧注释"返回无 nmId 无法批量"是未实测前的错误假设）→ 支持批量
- 新字段 `weekFrequency / avgPosition / openToCart / visibility / rating / price` 走 extra 兜底

**然后裸跑 `refresh_shop(shop=6, days=7)` 把 609 个 nm_id 轮了一遍** —— 这是灾难起点。WB search-texts 端点返回：

```
"title": "too many requests",
"detail": "Limited by global limiter, per seller f916ae65-a3dc-48f5-beca-fb737b0fe92b"
```

**"global limiter per seller"** = WB 对该 seller 在该接口的**累计 quota 限流**，不是分钟窗口。
我 08:09 点火一次烧光了 quota，之后 1h44min + 多次 probe 全部 429 重试 3 次失败（5+10+15s 退避）。

验证**只挡 search-texts 单接口**：同时用广告 `/adv/v1/promotion/count` 调 200 正常 → AI 调价 / 商品同步 / 广告数据全不受影响。

> **2026-04-23 晚间纠错（老林 review）**：上面"只挡 search-texts 单接口"**结论错误**。
> 实情：WB seller quota 是 **per-seller-per-endpoint-group 共享池**，我当时测 /promotion/count
> 返 200 只是因为那时 quota 还没完全烧光，不是端点独立。老林 §十七.2 用户立即分析
> 实测 PATCH bids + 读端点（fetch_campaign_products / 最低出价 / 关键词）**全部 429**
> 证伪了我的局部观察。
> **教训**：局部观察（"测 1 个端点没挡"）不能推广到所有端点。详见 memory
> `reference_wb_seller_quota_per_endpoint_group.md`。

### 11.2 批量 API 重构（`d12155f`）

旧假设"1 个 nm_id / 调用"作废，改批量：

```python
async def fetch_product_search_texts(self, nm_ids, date_from, date_to, ...):
    """nm_ids 接 int 或 list[int]；返回每条带 nm_id 字段，上层按 id 分组"""
```

上层 `refresh_shop` WB 分支改：50/批 → 后调小到 **20/批** + **20s 批间 sleep**。

### 11.3 Celery 任务 + beat（`d5903eb`）

`app/tasks/wb_search_texts_task.py` 参考 Ozon `ozon_product_queries_task` 模板：
- `sync_wb_search_texts` 遍历 active WB 店铺 → 复用 `refresh_shop`
- 93001 无 Jam 记 skipped 继续下一家
- 90 天前 WB 数据 cleanup
- `sync_wb_search_texts_for_shop(shop_id, tenant_id, days)` 单店手动/异步入口

Beat 新条目 `wb-search-texts-daily` `crontab(hour=4, minute=0)` —— Celery `timezone="Europe/Moscow"` 下直解为 **MSK 04:00** 触发（避免老 4 个任务"注释晚 3h"的坑，注释明确写清楚）。错开 Ozon MSK 02:30。

### 11.4 合规规则扫尾

- 规则 1：listings SQL 补 `pl.tenant_id` 纵深（2026-04-18 老张写漏了 JOIN 隐式过滤）`feaaaaf`
- 规则 6：`date.today()` → `moscow_today()` 3 处（OS 切 UTC 后 date.today 是 UTC 日期，业务用 MSK 日期）`daab978`

### 11.5 early exit 防空跑（`d1c9115`）

发现 quota 触发后继续跑 30 批 × 20s = 10min 全返 0 items 白费时间。加逻辑：**连续 3 批空 → break**，errors 里标 `early_exit=true + reason="quota"`。

单批可能正常空（商品确实没搜索词），连续 3 批空 = quota 信号，足够准确。

### 11.6 今日下午 commit

```
d12155f feat(search-insights): WB search-texts 批量 nmIds + 限流间隔
d5903eb feat(search-insights): WB 每日 Celery 任务 + beat MSK 04:00 调度
feaaaaf fix(search-insights): listings SQL 补 pl.tenant_id 纵深过滤
daab978 fix(search-insights): date.today() → moscow_today() 合规规则 6
0c8cdcc chore(search-insights): WB 批量 50→20 + pause 20s 更保守
d1c9115 feat(search-insights): refresh_shop 连续 3 批空 early exit
```

### 11.7 用户二次反馈：SEO Optimize WB 店铺也没数据

用户下午打开 SEO Optimize 页（不是搜索词洞察）说 WB 店铺没数据。诊断：SEO 的「店级关键词 TOP」+「按商品看聚合」和搜索词洞察**共用 `product_search_queries` 底表**（昨日老张 §二十八 架构如此）。shop=6 这张表目前 0 行 → `keyword_rollup_service.compute_keyword_rollup` 判两期全无数据 → 返 `not_ready`。

**结论给用户**：两个页面看到"WB 无数据"是同一个根因（quota 挡路），明早 MSK 04:00 Celery beat 自动跑完 → 两个页面同时都有数据。

### 11.8 遗留（明天）

**等 quota 自然恢复**：
1. 明早 MSK 04:00 Celery beat 自动触发 → 看日志确认 shop=6 入库 ≥数百行
2. 用户打开 SEO Optimize + 搜索词洞察页面 → 验证 ready 状态 + 数据合理
3. 若明早 beat 仍 quota 不足，再拖一天 —— 毕竟我下午 ~30 个调用都失败继续烧额度

**没做的 P1（用户拍"晚点做"）**：
- AI 调价 `new_bid == old_bid` 跳过 WB API 调用（今日 AI smoke 12/24 无变化还调了 API 浪费额度，也是 quota 消耗的部分原因）

### 11.9 下午体会（5 条）

1. **"把整个数据集一把梭跑起来"是最危险的自信**
   我明明一次裸调成功，就应该先 query 3-5 个验证批量解析，再跑 609 全量。结果 609 一气呵成把 quota 烧光。**端到端测试从 3 个点开始，不从 609 个开始**。

2. **"global limiter per seller" 是平台给的清晰信号**
   WB 错误 body 里明确写了限流类型（"global limiter per seller"）+ 文档链接，比一般 "429" 信息量大 10 倍。**读 error body 而不是只看 status code**，能快速分流根因。

3. **"一个 API 挂了不等于整个平台挂了"**
   广告 API 测 200 正常证明 quota 是 per-endpoint 的，立刻让用户放心"AI 调价等核心功能没挂"。**多测一个对照组，舆情管控范围大幅缩窄**。

4. **"两个页面共用一张表 = 一个故障两边显症"**
   搜索词洞察页和 SEO Optimize 页共用 `product_search_queries`，用户从 SEO 页发现症状 —— 用户往往从自己最常用的入口报 bug，不是从功能设计入口。**架构文档写再清楚，也不如跟着用户视角追根因快**。

5. **"Celery beat 自动触发是最稳的灰度发布"**
   quota 挡住我手动测试，但明早 MSK 04:00 自然日触发一次 —— 如果那时候成了，等于系统自己证实功能正常。**定时任务 = 免费的次日回归测试**，设计时就该留这条后路。

---

**老张** 2026-04-23 下午收工

上午 4 + 下午 6 = **10 commit + 2 段日志**。
上半天：AI 调价纵深 R1-R7 review / 索引清理 / 脏词黑名单
下半天：WB Jam 搜索词洞察批量改造 + Celery 任务 + 6 条规则合规 + early exit —— 等明早 quota 恢复后自动跑通。

---

## 十二、晚间：用户反馈 + 老林 review 双驱动（~3 commit）

### 12.1 用户反馈：SEO 优化建议 WB 店铺空白

> "SEO管理-优化建议，我选择了WB店铺，没有数据刷新出来"

**根因诊断**（2 层）：
- WB 3 店 `product_search_queries` 0 行（§11 下午 quota 挡路，预期内）
- WB 3 店 `seo_keyword_candidates` 也是 0 行（候选池引擎从没跑过；WB 无 ad_stats，
  源 A 失败；无 product_search_queries，源 B 失败 → 引擎两条路都空）
- **真正的 UX bug**：`compute_candidates_rollup` 对空表只返 `items: []`，
  前端只显示"当前条件下无候选词" —— 让用户以为功能坏了。对比
  `compute_keyword_rollup` 有 `data_status: not_ready` + hint 的规范做法。

**`addaf1d fix(seo): 按商品看聚合视图对 WB 等无候选池店给出 not_ready 提示`**：
- 后端 `compute_candidates_rollup` 加 `pool_count` 快速判，空则返
  `not_ready` + `_CANDIDATES_NOT_READY_HINT[platform]`（WB 指 Jam 订阅
  + MSK 04:00 同步；Ozon 指 Premium + MSK 02:30；Yandex 不支持）
- 前端 `CandidatesRollupTable` 多店合并时收集 `notReadyShops`，顶部 Alert
  列出各空店铺 + hint，**不遮盖其他正常店**的数据展示
- 生产 smoke 验证：WB shop=6/1/5 返 `not_ready`+hint，Ozon-Shario 返 `ready`+200 items

### 12.2 老林 review：WB quota pre-check 共用熔断 key

老林今晚 `ca5a96b feat(wb): seller quota 熔断扩到通用 _request` 后指出：
**我 §十一.1 "只挡 search-texts 单接口" 的结论错**（已在 §十一.1 下方纠错 +
memory `reference_wb_seller_quota_per_endpoint_group.md`）。配额是
seller-级跨端点共享池，写端点烧光读端点也 429。

真正的 bug：我 `refresh_shop` for 循环里 `except Exception` 吞掉
`WBSellerQuotaExhausted` 后 `continue` → 下一批再 sleep 20s 再 fail，30 批
× 20s = 10min 白烧；且明早 04:00 MSK beat 若熔断还活着必然白跑 1 整轮。

**`702362d feat(search-insights): refresh_shop WB 分支加 seller quota pre-check`**：
- 顶层 pre-check Redis 熔断 TTL，命中 0ms 返 `skipped: true`
- 循环内加 `WBSellerQuotaExhausted` 专捕 → 立即 break，不继续 sleep
- wb_search_texts_task 任务层识别 skipped 单独日志+结果归类
- 生产 smoke：shop=6 当前 cooldown 3457s，调 refresh_shop `elapsed=0.00s`

### 12.3 老林替我做掉的 P1

`6af3d4c + 9912400`：AI 调价 `new_bid==old_bid` 跳过 WB API 调用（含迁移 058
加 `last_optimal_bid` 字段）。API 节省 90%+，bidlog 只记真涨跌。我原 §十一.8
的"no-change skip"遗留可直接划掉。

### 12.4 互相约束（老林晚间提醒我，我也接受）

**今后任何 WB 全店扫描走三段式**：3 nm_id → 50 → 全店，不一把梭。
我早上 08:09 跑 609 是这次 quota 事故的起点；老林今天晚间也强调他也不再一次
smoke 25 SKU。

### 12.5 晚间 commit

```
addaf1d fix(seo): 按商品看聚合视图对 WB 等无候选池店给出 not_ready 提示
702362d feat(search-insights): refresh_shop WB 分支加 seller quota pre-check
(本) docs(daily): 老张 04-23 §十二 — 晚间用户反馈 + 老林 review 双驱动
```

### 12.6 明日优先级（更新版）

**P0 跟用户**：
- WB 后台"顶级搜索集群" CSV 样本（多日 P0 + 老林提醒）
- 明早 04:00 MSK WB beat 首跑是否产出数据（pre-check 未命中 → 真同步 → 产生
  product_search_queries → SEO 页 WB 店"按商品看"候选池仍需引擎再跑才有数据）

**P1 待议**：
- 候选池引擎 `analyze_paid_to_organic` 当前只有手动触发，WB 同步产出数据后
  需用户手点"刷新引擎"或加定时任务。如果用户想全自动，可加 beat
  `seo-engine-daily` MSK 05:00（在 WB/Ozon 同步后、日报前）
- `CandidatesRollupTable` 目前没有"刷新引擎"按钮入口（只有 SeoFilterBar
  的 `onRefresh`），rollup 模式下用户看到 not_ready 无处可点

### 12.7 晚间体会

1. **"错误结论比无结论更危险"** — 我 §十一.1 "只挡 search-texts 单接口" 写得
   很笃定，老林没这个 memory 就会以为 WB quota 是 per-endpoint。一个错结论
   会让整组人朝错方向走。**局部观察不能推广 —— 一个端点没挡不代表别的端点
   也没挡**。

2. **"熔断的覆盖面 = 最容易漏的一层"** — 老林 `ca5a96b` 把熔断挪到
   `_request` 就以为全通了，但我 `refresh_shop` 的 `except Exception` 吞掉了
   `WBSellerQuotaExhausted`，熔断信号被降级成"这批失败，下批再试"。
   **异常类型的语义必须保留 —— catch Exception 不是 catch 具体异常的放大版，
   是把所有异常降级为通用错误**。

3. **"用户问题是系统问题的最短路径"** — 用户"WB 店铺没数据"一句话暴露了
   `compute_candidates_rollup` 空态 UX + quota pre-check 漏 + 熔断被吞三层
   问题。**客服消息比 agent review 密度大**。

---

**老张** 2026-04-23 晚间收工

累计 **13 commit + 3 段日志**。晚间用户反馈 + 老林 review 双驱动落地两件：
SEO 空态提示对齐 / WB quota pre-check 共用熔断 key。明早 04:00 MSK 看 beat
首跑结果 + 跟用户要 CSV 样本。
