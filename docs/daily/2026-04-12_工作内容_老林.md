# 2026-04-12 工作内容 — 老林（系统架构师）

---

## 一、今日完成（37 个 commit，皆已 push 到 main）

### 1. 紧急修复：Celery 启动崩溃（17:24）

- **根因**：昨日重构 `ozon_stats_collector`（`sync_yesterday_stats` → `smart_sync`），但 `daily_sync_task.py` 未同步更新 import，导致 ImportError，Celery worker/beat 均 FATAL
- **修复**：`daily_sync_task.py` 改为按平台分发（`PLATFORM_SYNC = {"ozon": ozon_smart_sync, "wb": wb_smart_sync}`），同时扩展每日同步支持 WB 店铺
- 这是规则 3（删文件时清理孤儿依赖）的违反

### 2. datetime 存量违反全局清理（13:50）

- commit `2095849`：9 个文件、20 处 `datetime.utcnow()` → `datetime.now(timezone.utc)`
- grep 自查确认 `app/` 下零违反

### 3. AI 调价 WB 建议模式实现（14:12）

- `analyze_now` 支持 WB 平台，WB 强制走建议模式（跳过 auto_execute）
- `_call_ai()` 对 WB 用 `bid_search`（卢布）+ `subject_name`
- `_execute_bid_update` 区分 Ozon（微卢布×1000000）和 WB（直接卢布）

### 4. 分时调价 WB 平台支持（14:23 - 14:47）

- 分时调价支持 WB 平台 + 使用说明面板 + 关闭确认弹窗
- 文案按平台动态显示 + 默认时段对齐后端

### 5. 前端 AI 调价交互优化（14:54 - 15:18）

- AI 调价 4 按钮改成 2 个 Switch 开关
- 冲突提示统一为 Modal 弹窗风格

### 6. 数据同步重构（15:18 - 16:44）

- Ozon 数据源重构为智能同步逻辑（无数据拉7天/有数据增量补/清理90天前）
- **WB SKU 级数据采集实现**（新功能）：fullstats 接口 + nm_id 级写入
- ROAS 溢出截断 + 异常 rollback
- 数据同步超时改为 5 分钟 + 每次最多拉 7 天 + 更新中/完成提示
- Excel 增加商品 SKU 列 + 按平台过滤 + 表头加单位
- 响应拦截器跳过 blob 类型（修复文件下载失败）
- AI 分析接口超时改为 120 秒

### 7. AI Prompt 全面增强（17:58 - 20:36）

- **stats_7d 新增 CPC + 转化率**：从已有数据算出，不需额外查库
- **策略模板精简**：去掉 `daily_budget` 和 `gross_margin`（对 AI 决策无实际约束力），前后端同步清理
- **ROAS 三档决策**：
  - ROAS >= 目标 → 加价扩量
  - 最低 <= ROAS < 目标 → 小幅降价控成本
  - ROAS < 最低 → 大幅降价止损
- 策略模板切换/编辑后自动保存到后端
- 数据源管理移到 AI 开关上方（操作逻辑：先看数据 → 再开 AI）
- `data_days` 改为从 `ad_stats` 直接查（不依赖 AI 返回）

### 8. AI 分析流式输出（20:17 - 20:25）

- `BaseAIClient` 新增 `chat_stream()` 流式接口
- `ai_pricing_executor` 新增 `analyze_stream()` SSE 生成器
- 后端新增 `GET /ai-pricing/{shop_id}/analyze-stream` SSE 端点
- 前端 Modal 弹窗实时展示 AI 分析过程：
  - 阶段提示（读取配置 → 拉商品 → 查历史 → AI 分析 → 保存建议）
  - 建议卡片逐条出现（商品名 + 阶段 Tag + 出价变化 + reason）
  - 分析中遮罩禁止操作页面

### 9. 建议列表 UI 改进（18:39 - 20:56）

- 去掉忽略按钮（重新分析自动清旧建议）+ 表头全选 checkbox
- "执行" → "手动执行"
- checkbox 合并到活动/商品列，与活动名左对齐
- WB 商品显示图片（从 nm_id 计算 CDN 地址，自动探测 basket）
- 建议出价改为可编辑 InputNumber，用户可手动修改后再执行
- 重复建议修复：每次分析前清掉所有 pending（不仅昨天的）

### 10. WB 出价执行健壮性（21:01 - 21:26）

- approve 接口支持传入用户修改后的出价（`override_bid` 参数）
- WB 错误消息透传平台原文（如 `wrong bid value: 5600; min: 8000`）
- **WB 出价低于最低限额时自动用 min 值重试**（从错误消息解析 min 值）
- 修复 `Optional` import 缺失导致 FastAPI 启动崩溃 502

### 11. 数据修正

- `shop_data_init_status.data_days` 被旧版 `daily_sync_task` 覆盖为 0，手动修正为实际 30 天
- 清空重复的 pending 建议

---

## 二、遗留（明天要处理）

### A. Ozon API 凭证失效

- Ozon 店铺（shop_id=2）Performance API 返回 401、Seller API 返回 403
- 导致 Ozon 的 ad_stats 完全没有数据，数据源下载也为空
- **明日做**：用户去 Ozon 商家后台重新生成 API 凭证，在系统店铺设置里更新

### B. WB 商品图片部分不显示

- WB CDN basket 编号规则不完全准确，高 vol 段映射偏移
- 当前用自动探测方案（估算 basket ±4 尝试），大部分能出但不是 100%
- **后续优化**：可以在后端缓存已验证的 nm_id → basket 映射

### C. AI 建议出价与平台最低限额冲突

- 已实现自动用 min 值重试，但 AI 不知道平台最低限额，会生成无效建议
- **后续优化**：在 prompt 里告知 AI 当前活动的最低出价（需要从 WB API 预查）

### D. WB 出价修改端到端回归

- 今天改了 approve 流程的 min bid 自动重试，需要验证整个链路：
  AI 建议 → 用户确认 → 后端调 WB API → 出价生效 → 3 分钟后回读验证

### E. Ozon AI 调价端到端验证

- Ozon 凭证修复后，需要验证 Ozon 店铺的 AI 调价完整链路

---

## 三、明天计划（按优先级）

| # | 任务 | 阻塞 |
|---|------|------|
| 1 | Ozon API 凭证修复（等用户提供新凭证） | 用户操作 |
| 2 | WB 出价修改端到端回归验证 | 无 |
| 3 | Ozon AI 调价端到端验证（凭证修复后） | 依赖 #1 |
| 4 | WB 商品图片 basket 缓存方案 | 无 |
| 5 | AI prompt 增加平台最低出价参数 | 无 |

---

## 四、今日 commit 清单（按时间正序，共 37 个）

```
2095849 13:50 fix: 全局清理 datetime.utcnow() → datetime.now(timezone.utc)
6e3d15d 14:12 feat(ai-pricing): WB 建议模式实现 — analyze_now 支持 WB 平台
76f8e05 14:23 feat(bid-management): 分时调价增加使用说明面板 + 关闭确认弹窗
5bd6744 14:30 feat(bid-management): 分时调价支持 WB 平台
4c1f515 14:38 fix(bid-management): WB 店铺出价管理放开 + 分时调价平谷期提示
c23fc42 14:47 fix(bid-management): 出价管理文案按平台动态显示 + 默认时段对齐后端
7e99147 14:49 chore: CLAUDE.md 增加架构师启动流程 + 小步提交规范
5e155a5 14:54 进度：AI调价4按钮改成2个Switch开关
373cb3d 14:58 进度：AI调价合并成一个Switch开关，自动执行挪到策略模板内
54303a2 15:02 进度：AI调价合成一个Switch，删除自动执行UI
773ca8a 15:06 进度：AI调价冲突提示统一为Modal弹窗风格
cf5e373 15:18 feat(data-sync): 重构Ozon数据源为智能同步逻辑
800e512 15:51 feat(data-sync): WB SKU 级数据采集实现
937da3c 16:02 fix(wb): fullstats 参数修正
15ca62a 16:04 fix(wb): ROAS溢出截断 + 数据采集异常rollback
1187e8d 16:08 feat(ai-pricing): prompt 增加 SKU 最近7天历史数据
8e32f49 16:11 fix(data-download): Excel增加商品SKU列 + 按平台过滤 + 表头加单位
f7d9ac9 16:14 fix: 响应拦截器跳过 blob 类型（修复文件下载失败）
4adf0ba 16:28 fix: 数据同步接口超时改为5分钟
7d8cad6 16:36 feat(data-sync): 每次最多拉7天 + 更新中/完成提示
c2f2e3b 16:44 fix: AI分析接口超时改为120秒
fbc0360 17:24 fix: 修复Celery启动崩溃（daily_sync_task引用已重构的sync_yesterday_stats）
4fff250 17:58 进度：AI prompt增加CPC+转化率 + 补全daily_budget + 强化策略说明
351aecf 18:01 refactor: 策略模板去掉 daily_budget 和 gross_margin
c8362cf 18:39 feat: 策略模板切换/编辑后自动保存到后端
4aec3a7 18:40 fix: 数据源管理移到AI智能调价开关上方
a33d552 18:43 fix: 建议列表data_days从历史数据直接取
62fe55e 18:54 feat: 建议列表去掉忽略按钮 + 表头全选checkbox
a2e2f45 20:17 feat: AI分析流式输出 — DeepSeek思考过程
7049c04 20:21 fix: AI分析改为Modal弹窗 + 修复401认证
5aaf4b3 20:19 fix: 建议列表"执行"改为"手动执行"
f9f4706 20:25 feat: AI分析弹窗改为可读卡片式UI
206586a 20:36 fix: AI调价ROAS判断改为三档
c07aaaa 20:40 fix: 立即分析前清掉所有pending建议
b4ad6a4 20:46 feat: 建议列表WB商品显示图片
8c4ac43 20:49 fix: WB商品图片basket范围扩展
2722a0e 20:50 fix: checkbox合并到活动/商品列
981db6a 20:56 fix: WB图片URL + 建议出价改为可编辑InputNumber
25fd5e9 21:01 fix: WB出价错误透传真实原因 + 图片自动探测basket
d67b284 21:05 feat: 手动执行支持用户修改后的出价
133e84d 21:21 fix: 补 Optional import（FastAPI启动崩溃502）
4089979 21:26 fix: WB出价低于最低限额时自动用min值重试
```

---

**老林**  2026-04-12
